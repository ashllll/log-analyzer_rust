---
phase: 01-architecture-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - log-analyzer_flutter/lib/shared/providers/app_provider.dart
  - log-analyzer_flutter/lib/main.dart
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - "Riverpod ProviderScope 正确包裹应用"
    - "AppProvider 初始化逻辑正确"
    - "Provider 可以正常解析和注入依赖"
  artifacts:
    - path: "log-analyzer_flutter/lib/shared/providers/app_provider.dart"
      provides: "应用级 Provider"
---

<objective>
验证 Riverpod Provider 基础配置正常工作，更新 AppProvider 适配新的 FFI 初始化流程。

Purpose: 确保 Riverpod Provider 正确配置，AppProvider 与新的 FFI 初始化流程集成。
Output: 验证通过的 Provider 配置
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-architecture-infrastructure/01-CONTEXT.md
@log-analyzer_flutter/lib/shared/providers/app_provider.dart
@log-analyzer_flutter/lib/main.dart
@log-analyzer_flutter/lib/shared/services/api_service.dart

---

## Riverpod 配置（来自 RESEARCH.md）

- 使用 Riverpod 3.0 AsyncNotifier
- 文件组织：单文件模块（workspace_provider.dart, search_provider.dart 等）
- 依赖注入：Provider 构造函数注入依赖
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 AppProvider 适配新的 FFI 初始化流程</name>
  <files>log-analyzer_flutter/lib/shared/providers/app_provider.dart</files>
  <action>
更新 AppProvider 以适配新的 FFI 初始化流程（由 SplashPage 处理）:

```dart
// lib/shared/providers/app_provider.dart
import 'dart:async';

import 'package:flutter/foundation.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';

import '../models/app_state.dart';
import '../services/api_service.dart';
import '../../core/constants/app_constants.dart' show AppPage, ToastType;

part 'app_provider.g.dart';

/// 应用状态 Provider
///
/// 管理应用级别的全局状态
/// FFI 初始化由 SplashPage 处理，此 Provider 负责配置加载
@riverpod
class AppState extends _$AppState {
  @override
  AppModel build() {
    // 不在此处初始化 FFI，由 SplashPage 负责
    // 只负责配置加载
    return const AppModel();
  }

  /// 加载配置
  ///
  /// 在 FFI 初始化完成后调用
  Future<void> loadConfig() async {
    try {
      final apiService = ref.read(apiServiceProvider);

      if (!apiService.isFfiAvailable) {
        debugPrint('AppState: FFI 桥接不可用，跳过加载配置');
        return;
      }

      await apiService.loadConfig();
      debugPrint('AppState: 配置加载完成');
    } catch (e) {
      debugPrint('AppState: 加载配置失败: $e');
      // 配置加载失败不影响应用运行
    }
  }

  /// 切换页面
  void setPage(AppPage page) {
    state = state.copyWith(currentPage: page);
  }

  /// 添加 Toast 消息
  void addToast(ToastType type, String message, {int? duration}) {
    final toast = Toast(
      id: DateTime.now().millisecondsSinceEpoch.toString(),
      type: type,
      message: message,
      duration: duration ?? 3000,
    );
    state = state.copyWith(toasts: [...state.toasts, toast]);

    if (toast.duration != null) {
      Future.delayed(Duration(milliseconds: toast.duration!), () {
        removeToast(toast.id);
      });
    }
  }

  /// 移除 Toast 消息
  void removeToast(String id) {
    state = state.copyWith(
      toasts: state.toasts.where((t) => t.id != id).toList(),
    );
  }

  /// 设置活动工作区
  void setActiveWorkspace(String? id) {
    state = state.copyWith(activeWorkspaceId: id);
  }

  /// 清除所有 Toast
  void clearAllToasts() {
    state = state.copyWith(toasts: []);
  }
}
```

主要变更:
- 移除 `_initializeApp()` 方法中的 `await ApiService.initialize()`
- 保留 `loadConfig()` 方法供 SplashPage 成功后调用
- AppProvider 现在不负责 FFI 初始化
  </action>
  <verify>flutter analyze lib/shared/providers/app_provider.dart 无错误</verify>
  <done>AppProvider 已更新，正确适配新的 FFI 初始化流程</done>
</task>

<task type="auto">
<name>Task 2: 更新 SplashPage 成功后调用 AppProvider.loadConfig</name>
<files>log-analyzer_flutter/lib/features/splash/splash_page.dart</files>
  <action>
更新 SplashPage，在 FFI 初始化成功后调用 AppProvider.loadConfig():

```dart
// 在 SplashPage._initialize() 方法中，初始化成功后添加:
Future<void> _initialize() async {
  try {
    await BridgeService.instance.initialize().timeout(_timeout);

    if (mounted) {
      // 初始化成功后加载配置
      ref.read(appStateProvider.notifier).loadConfig();

      // 跳转到主页
      context.go('/home');
    }
  } catch (e) {
    // ... 错误处理
  }
}
```

在文件顶部添加导入:
```dart
import '../../shared/providers/app_provider.dart';
```
  </action>
  <verify>grep -n "loadConfig" log-analyzer_flutter/lib/features/splash/splash_page.dart</verify>
  <done>SplashPage 成功后调用配置加载</done>
</task>

<task type="auto">
  <name>Task 3: 验证 Flutter 应用可以正常编译</name>
  <files>log-analyzer_flutter</files>
  <action>
运行 Flutter 分析验证代码无错误:

```bash
cd log-analyzer_flutter
flutter analyze
```

确保:
1. 所有 Provider 正确导入
2. 无类型错误
3. FFI 初始化流程正确

如果分析失败，根据错误信息修复。
  </action>
  <verify>flutter analyze lib/ 无错误</verify>
  <done>Flutter 应用编译验证通过</done>
</task>

</tasks>

<verification>
- [ ] AppProvider 已更新，FFI 初始化由 SplashPage 负责
- [ ] SplashPage 初始化成功后调用配置加载
- [ ] flutter analyze 无错误
</verification>

<success_criteria>
Riverpod Provider 配置正确，应用可以正常编译
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-infrastructure/01-04-SUMMARY.md`
</output>
