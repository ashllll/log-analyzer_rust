---
phase: 02-workspace-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart"
  - "log-analyzer_flutter/lib/shared/providers/workspace_provider.dart"
  - "log-analyzer_flutter/lib/shared/models/common.dart"
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
user_setup: []

must_haves:
  truths:
    - "用户可以创建新的工作区（带自定义名称和文件夹选择）"
    - "用户可以打开已有工作区（点击卡片）"
    - "用户可以删除工作区（带确认对话框）"
    - "用户可以查看工作区状态（文件数、索引状态、总大小）"
    - "用户可以使用键盘导航（上下键选择，回车打开）"
    - "最近打开的工作区显示在最前"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart"
      provides: "工作区卡片列表、创建对话框、删除确认、键盘导航"
      contains: "WorkspacesPage, _WorkspaceCard, _AddWorkspaceDialog"
    - path: "log-analyzer_flutter/lib/shared/providers/workspace_provider.dart"
      provides: "工作区状态管理、CRUD 操作"
      contains: "WorkspaceState provider"
  key_links:
    - from: "workspaces_page.dart"
      to: "workspace_provider.dart"
      via: "ref.watch(workspaceStateProvider)"
      pattern: "workspaceStateProvider"
---

<objective>
增强工作区管理功能，包括键盘导航支持和最近工作区排序。
</objective>

<context>
@.planning/phases/02-workspace-import/02-CONTEXT.md
@.planning/phases/02-workspace-import/02-RESEARCH.md
@log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart
@log-analyzer_flutter/lib/shared/providers/workspace_provider.dart
@log-analyzer_flutter/lib/shared/models/common.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加键盘导航支持</name>
  <files>log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart</files>
  <action>
为工作区列表添加键盘导航支持：
1. 使用 Focus 组件包裹 ListView，启用键盘焦点
2. 添加 FocusNode 管理焦点状态
3. 实现上下键选择工作区
4. 实现回车键打开选中的工作区
5. 使用 RawKeyboardListener 或 Shortcuts widget 处理键盘事件
6. 保持与现有鼠标/触摸交互的兼容性
  </action>
  <verify>
    <automated>flutter test test/workspace/workspace_provider_test.dart</automated>
  </verify>
  <done>在无鼠标情况下，用户可以使用上下键选择工作区，回车键打开</done>
</task>

<task type="auto">
  <name>Task 2: 实现最近优先排序</name>
  <files>log-analyzer_flutter/lib/shared/providers/workspace_provider.dart</files>
  <action>
修改工作区加载和存储逻辑，实现最近优先排序：
1. 在 Workspace 模型中添加 lastOpenedAt 字段（可选）
2. 修改 loadWorkspaces() 方法，按 lastOpenedAt 降序排序
3. 当用户打开工作区时更新 lastOpenedAt 时间戳
4. 持久化排序信息到本地存储（shared_preferences）
5. 最近打开的 3 个工作区显示在最前
  </action>
  <verify>
    <automated>flutter test test/workspace/workspace_provider_test.dart</automated>
  </verify>
  <done>工作区列表按最近打开时间排序，最近的显示在最前</done>
</task>

<task type="auto">
  <name>Task 3: 增强工作区状态显示</name>
  <files>log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart
log-analyzer_flutter/lib/shared/models/common.dart</files>
  <action>
增强工作区状态显示：
1. 添加工作区创建时间显示
2. 优化状态指示器颜色和图标（就绪/索引中/错误）
3. 添加状态轮询机制，定期更新工作区状态
4. 在卡片悬停时显示操作按钮（已有部分实现）
5. 确保状态变化时 UI 自动更新
  </action>
  <verify>
    <automated>flutter test test/workspace/workspace_provider_test.dart</automated>
  </verify>
  <done>用户可以看到完整的工作区信息，包括创建时间、状态、文件数量、总大小</done>
</task>

</tasks>

<verification>
1. 启动 Flutter 应用：cd log-analyzer_flutter && flutter run
2. 导航到工作区页面
3. 验证键盘导航：使用上下键选择，回车打开
4. 验证排序：创建多个工作区并打开，观察排序
5. 验证状态显示：检查卡片信息完整性
</verification>

<success_criteria>
- [x] 工作区卡片布局完整显示
- [x] 键盘导航正常工作
- [x] 最近工作区优先排序
- [x] 状态指示器正确显示
</success_criteria>

<output>
完成后创建 `.planning/phases/02-workspace-import/02-01-SUMMARY.md`
</output>
