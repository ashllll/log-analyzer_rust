## 步骤2: 修复 error.rs 中 Io 和 IoDetailed 错误类型冗余

**任务**: 合并 `AppError::Io` 和 `AppError::IoDetailed` 两个错误类型

**修改内容**:
- 保留 `AppError::Io` 作为主要的 IO 错误类型
- 修改 `AppError::Io` 使其包含可选的 `path` 字段
- 移除冗余的 `AppError::IoDetailed` 变体
- 更新所有使用 `IoDetailed` 的地方改为使用 `Io`

**验证方法**:
- 运行 `cargo check` 确保编译通过
- 运行 `cargo test` 确保测试通过

**影响范围**:
- 修改错误定义（向后兼容）
- 影响约 3-5 处使用点