---
phase: 05-realtime-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/realtime_monitoring/providers/monitoring_provider.dart
  - log-analyzer_flutter/lib/features/realtime_monitoring/models/monitoring_state.dart
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03

must_haves:
  truths:
    - "用户可以通过工具栏按钮启用/禁用文件监控"
    - "监控状态变化时 UI 实时更新"
    - "用户可以在状态面板查看监控详细信息"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/realtime_monitoring/providers/monitoring_provider.dart"
      provides: "Riverpod状态管理"
      min_lines: 50
    - path: "log-analyzer_flutter/lib/features/realtime_monitoring/models/monitoring_state.dart"
      provides: "监控状态数据模型"
      contains: "class MonitoringState"
  key_links:
    - from: "monitoring_provider.dart"
      to: "bridge_service.dart"
      via: "BridgeService.startWatch/stopWatch"
      pattern: "startWatch|stopWatch"
---

<objective>
实现实时监控的核心状态管理与事件处理机制

Purpose: 为实时监控功能提供状态管理和事件处理基础设施
Output: MonitoringProvider + MonitoringState 模型
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# 现有基础设施（来自 RESEARCH.md）

## Rust 后端
- `start_watch` 命令: `commands/watch.rs` - 创建 notify watcher
- `stop_watch` 命令: 停止监控
- `file-changed` 事件: 文件变化时发出

## Flutter 前端
- BridgeService: `bridge_service.dart:385-435` - FFI 方法存在
- EventStreamService: `event_stream_service.dart` - 处理 FileChanged 事件

## 关键类型（从 event_stream_service.dart）
```dart
class FileChangeEvent {
  final String path;
  final String eventType; // create, modify, remove
  final DateTime timestamp;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建监控状态数据模型</name>
  <files>log-analyzer_flutter/lib/features/realtime_monitoring/models/monitoring_state.dart</files>
  <action>
创建 MonitoringState 数据模型，包含以下字段：
- isActive: bool - 监控是否启用
- eventsProcessed: int - 已处理的事件数
- pendingCount: int - 待处理队列数量
- monitoredDirsCount: int - 监控的目录数
- monitoredFilesCount: int - 监控的文件数
- lastUpdate: DateTime? - 最后更新时间
- errorMessage: String? - 错误信息（如有）

包含 copyWith 方法用于不可变更新。
  </action>
  <verify>文件存在且包含 MonitoringState 类，字段完整</verify>
  <done>MonitoringState 类可实例化，包含所有必需字段</done>
</task>

<task type="auto">
  <name>Task 2: 实现监控状态 Provider</name>
  <files>log-analyzer_flutter/lib/features/realtime_monitoring/providers/monitoring_provider.dart</files>
  <action>
创建 MonitoringNotifier (StateNotifier) 实现：
1. 构造函数初始化状态为非活动状态
2. startMonitoring(workspaceId, paths): 调用 BridgeService.startWatch，设置 isActive=true
3. stopMonitoring(workspaceId): 调用 BridgeService.stopWatch，设置 isActive=false
4. onFileChanged(event): 处理文件变化事件，更新 eventsProcessed 和 lastUpdate
5. addToQueue(event): 将事件加入待处理队列（实现 500ms 防抖和限流）
6. 队列最大长度设为 1000，超出则丢弃旧请求
7. 使用 Timer 实现 500ms 防抖
8. 使用计数器实现每秒最多 10 个文件的限流
9. 重试逻辑：失败时重试 3 次，间隔 100ms, 200ms, 400ms

实现变更处理：
- create 事件：调用索引添加文件
- modify 事件：更新索引
- remove 事件：从索引移除

监听 EventStreamService 的 fileChanged 事件。
  </action>
  <verify>cargo test --all-features 通过，flutter analyze 无错误</verify>
  <done>MonitoringNotifier 包含完整的启动/停止/事件处理逻辑</done>
</task>

</tasks>

<verification>
- MonitoringState 模型可实例化
- MonitoringNotifier 可处理 start/stop/fileChanged 事件
- 防抖和限流逻辑正确实现
</verification>

<success_criteria>
1. MonitoringProvider 正确调用 BridgeService 启动/停止监控
2. 文件变化事件能正确更新状态
3. 防抖 (500ms) 和限流 (10/秒) 正确实现
4. 队列管理正确（最大 1000 条）
</success_criteria>

<output>
完成后创建 `.planning/phases/05-realtime-monitoring/05-01-SUMMARY.md`
</output>
