---
phase: 05-realtime-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_toolbar_button.dart
  - log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_status_panel.dart
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03

must_haves:
  truths:
    - "用户可以通过工具栏按钮启用/禁用监控"
    - "按钮图标根据监控状态显示不同颜色"
    - "用户可以在状态面板查看监控详情"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_toolbar_button.dart"
      provides: "监控开关按钮"
      min_lines: 30
    - path: "log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_status_panel.dart"
      provides: "监控状态面板"
      min_lines: 50
  key_links:
    - from: "monitoring_toolbar_button.dart"
      to: "monitoring_provider.dart"
      via: "ref.watch/monitoringProvider"
      pattern: "monitoringProvider"
---

<objective>
实现实时监控的 UI 组件

Purpose: 提供监控开关按钮和状态面板
Output: MonitoringToolbarButton + MonitoringStatusPanel
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-realtime-monitoring/05-01-PLAN.md

# UI 组件设计（来自 CONTEXT.md）

## 监控按钮
- 工具栏按钮启用/禁用文件监控
- 眼睛图标根据状态变化颜色（绿色=监控中，红色=停止）
- 按工作区独立控制
- 应用重启后默认关闭

## 状态面板
- 专门的状态面板显示（不在工具栏或侧边栏）
- 完整信息显示：监控状态、活动指示器、事件计数
- 实时显示处理的事件数和待处理数
- 显示正在监控的目录和文件数量
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现监控开关按钮</name>
  <files>log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_toolbar_button.dart</files>
  <action>
创建 MonitoringToolbarButton 组件：
1. 使用 Riverpod 的 ref.watch 监听 monitoringProvider
2. 显示眼睛图标 (Icons.visibility / Icons.visibility_off)
3. 监控状态为 true 时显示绿色 (Colors.green)
4. 监控状态为 false 时显示红色 (Colors.red)
5. 点击时调用 monitoringProvider.startMonitoring 或 stopMonitoring
6. 显示 Tooltip 说明当前状态
7. 可以打开/关闭状态面板（通过 Provider 控制）

按钮样式：
- 使用 IconButton 组件
- 颜色根据状态动态变化
- 点击切换监控状态
</action>
  <verify>flutter analyze 无错误，按钮组件可渲染</verify>
  <done>按钮可点击，点击后切换监控状态，图标颜色正确变化</done>
</task>

<task type="auto">
  <name>Task 2: 实现监控状态面板</name>
  <files>log-analyzer_flutter/lib/features/realtime_monitoring/presentation/widgets/monitoring_status_panel.dart</files>
  <action>
创建 MonitoringStatusPanel 组件：
1. 使用 Riverpod 的 ref.watch 监听 monitoringProvider
2. 布局设计（Claude's Discretion - 自由设计）：
   - 卡片式布局，显示在主内容区域或浮动面板
   - 显示以下信息：
     * 监控状态：运行中 / 已停止
     * 活动指示器：监控中显示动画圆点
     * 已处理事件数：eventsProcessed
     * 待处理数：pendingCount
     * 监控目录数：monitoredDirsCount
     * 监控文件数：monitoredFilesCount
     * 最后更新时间：lastUpdate
   - 显示错误信息（如果有）

3. 状态面板可以通过按钮打开/关闭
4. 实时更新（使用 ref.watch 自动刷新）
5. 空的占位状态（监控未启用时显示提示）

面板布局建议：
- 使用 Column 垂直排列信息
- 使用 ListTile 或自定义 Row 显示每个指标
- 使用 Card 包裹整体
</action>
  <verify>flutter analyze 无错误，状态面板可渲染</verify>
  <done>状态面板正确显示所有监控信息，数据实时更新</done>
</task>

</tasks>

<verification>
- MonitoringToolbarButton 正确响应监控状态变化
- MonitoringStatusPanel 显示完整监控信息
- UI 与 Provider 正确连接
</verification>

<success_criteria>
1. 工具栏按钮可启用/禁用监控
2. 眼睛图标颜色正确变化（绿色/红色）
3. 状态面板显示所有必要信息
4. UI 实时响应状态变化
</success_criteria>

<output>
完成后创建 `.planning/phases/05-realtime-monitoring/05-02-SUMMARY.md`
</output>
