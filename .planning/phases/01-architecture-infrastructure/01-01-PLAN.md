---
phase: 01-architecture-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "log-analyzer_flutter/lib/shared/services/bridge_service.dart"
  - "log-analyzer_flutter/lib/shared/services/api_service.dart"
  - "log-analyzer_flutter/pubspec.yaml"
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - "Flutter 应用使用纯 FFI 模式与 Rust 后端通信"
    - "BridgeService 移除 Dio HTTP 依赖"
    - "FFI 延迟加载在首次调用时初始化"
  artifacts:
    - path: "log-analyzer_flutter/lib/shared/services/bridge_service.dart"
      provides: "纯 FFI 桥接服务"
      contains: "flutter_rust_bridge"
  key_links:
    - from: "api_service.dart"
      to: "bridge_service.dart"
      via: "BridgeService 实例"
---

<objective>
将 BridgeService 从 HTTP 客户端模式重构为纯 FFI 模式，使用延迟加载策略。

Purpose: 根据 CONTEXT.md 决策，仅使用 FFI 与 Rust 后端通信，移除 HTTP 备选方案。
Output: 重构后的 BridgeService 使用 flutter_rust_bridge FFI 调用
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-architecture-infrastructure/01-CONTEXT.md
@.planning/phases/01-architecture-infrastructure/01-RESEARCH.md
@log-analyzer_flutter/lib/shared/services/bridge_service.dart (当前使用 Dio HTTP 客户端)
@log-analyzer_flutter/lib/shared/services/generated/frb_generated.dart (FFI 生成代码)

---

## 当前状态

BridgeService 当前配置:
```dart
const bool _useHttpClient = true;  // 需要改为 false
```

FFI 生成代码已存在于: `lib/shared/services/generated/`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 pubspec.yaml 移除 Dio 依赖</name>
  <files>log-analyzer_flutter/pubspec.yaml</files>
  <action>
移除 Dio HTTP 客户端依赖。根据 CONTEXT.md 决策：纯 FFI 模式，不保留 HTTP 备选。

在 pubspec.yaml 中删除:
```yaml
# HTTP 客户端 - 用于调用 Rust HTTP API
dio: ^5.4.0
```

保留 flutter_rust_bridge 依赖（已存在）。
  </action>
  <verify>grep -n "dio" log-analyzer_flutter/pubspec.yaml 返回空</verify>
  <done>Dio 依赖已从 pubspec.yaml 中移除</done>
</task>

<task type="auto">
  <name>Task 2: 重构 BridgeService 为纯 FFI 模式</name>
  <files>log-analyzer_flutter/lib/shared/services/bridge_service.dart</files>
  <action>
将 BridgeService 从 HTTP 客户端重构为纯 FFI 模式:

1. 移除 Dio 导入和 HTTP 相关代码
2. 使用 flutter_rust_bridge 的 FFI 生成代码 (`LogAnalyzerBridge`)
3. 实现延迟加载 - 首次调用时初始化 FFI
4. 移除 `_useHttpClient` 常量（或设为 false）
5. 添加 FFI 初始化状态管理

参考 RESEARCH.md 中的 Pattern 1: FFI Bridge Service (延迟加载)

```dart
import 'package:flutter_rust_bridge/flutter_rust_bridge.dart';
import 'generated/frb_generated.dart';

class BridgeService {
  static BridgeService? _instance;
  static bool _isInitialized = false;
  static bool _initFailed = false;

  BridgeService._();

  static BridgeService get instance {
    _instance ??= BridgeService._();
    return _instance!;
  }

  /// 延迟初始化 FFI
  Future<void> initialize() async {
    if (_isInitialized) return;
    if (_initFailed) throw FfiInitializationException('FFI 初始化已失败');

    try {
      await LogAnalyzerBridge.init();
      _isInitialized = true;
      debugPrint('FFI Bridge 初始化成功');
    } catch (e) {
      _initFailed = true;
      debugPrint('FFI Bridge 初始化失败: $e');
      rethrow;
    }
  }

  bool get isFfiEnabled => _isInitialized && !_initFailed;
}
```

注意: FFI 方法调用需要根据生成的 `frb_generated.dart` 中的 API 调整。
  </action>
  <verify>grep -n "flutter_rust_bridge" log-analyzer_flutter/lib/shared/services/bridge_service.dart</verify>
  <done>BridgeService 使用纯 FFI 模式，移除 Dio HTTP 调用</done>
</task>

<task type="auto">
  <name>Task 3: 更新 ApiService 使用新的 BridgeService</name>
  <files>log-analyzer_flutter/lib/shared/services/api_service.dart</files>
  <action>
更新 ApiService 以使用重构后的纯 FFI BridgeService:

1. 更新 BridgeService 实例化方式
2. 更新 FFI 初始化调用方式
3. 更新 `isFfiAvailable` 属性检查
4. 移除对 Dio 的任何残留引用（如有）

ApiService 中的关键变更:
```dart
class ApiService {
  final BridgeService _bridge = BridgeService.instance;

  static bool _isInitialized = false;

  static Future<void> initialize() async {
    if (_isInitialized) return;
    try {
      await BridgeService.instance.initialize();
      _isInitialized = true;
    } catch (e) {
      debugPrint('API Service 初始化失败: $e');
    }
  }

  bool get isFfiAvailable => _bridge.isFfiEnabled && _isInitialized;
}
```
  </action>
  <verify>flutter analyze lib/shared/services/api_service.dart 无错误</verify>
  <done>ApiService 正确使用纯 FFI BridgeService</done>
</task>

</tasks>

<verification>
- [ ] pubspec.yaml 中无 Dio 依赖
- [ ] BridgeService 使用 flutter_rust_bridge FFI 调用
- [ ] FFI 延迟加载实现正确
- [ ] ApiService 正确调用 BridgeService
</verification>

<success_criteria>
Flutter 应用通过 FFI 与 Rust 后端通信，无 HTTP 依赖
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-infrastructure/01-01-SUMMARY.md`
</output>
