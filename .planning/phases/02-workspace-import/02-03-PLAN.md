---
phase: 02-workspace-import
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - "log-analyzer_flutter/lib/shared/services/api_service.dart"
  - "log-analyzer_flutter/lib/shared/widgets/archive_import_dialog.dart"
  - "log-analyzer_flutter/lib/shared/widgets/drop_zone.dart"
autonomous: true
requirements:
  - FILE-02
  - FILE-03
  - FILE-04
  - FILE-05
  - FILE-06
user_setup: []

must_haves:
  truths:
    - "用户可以导入 ZIP 压缩包"
    - "用户可以导入 TAR 压缩包"
    - "用户可以导入 GZIP 压缩包"
    - "用户可以导入 RAR 压缩包"
    - "用户可以导入 7Z 压缩包"
    - "压缩包自动检测格式"
    - "用户可以预览压缩包内容"
  artifacts:
    - path: "log-analyzer_flutter/lib/shared/services/api_service.dart"
      provides: "压缩包导入 API 方法"
      contains: "importArchive, listArchiveContents"
    - path: "log-analyzer_flutter/lib/shared/widgets/archive_import_dialog.dart"
      provides: "压缩包导入对话框（预览、选择解压）"
  key_links:
    - from: "archive_import_dialog.dart"
      to: "api_service.dart"
      via: "apiService.importArchive()"
      pattern: "importArchive"
    - from: "drop_zone.dart"
      to: "archive_import_dialog.dart"
      via: "onArchiveDropped()"
      pattern: "ArchiveImportDialog"
---

<objective>
实现压缩包导入支持（ZIP/TAR/GZ/RAR/7Z），包括预览和选择性解压功能。
</objective>

<context>
@.planning/phases/02-workspace-import/02-CONTEXT.md
@.planning/phases/02-workspace-import/02-RESEARCH.md
@log-analyzer_flutter/lib/shared/services/api_service.dart
@log-analyzer/src-tauri/src/commands/import.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加压缩包导入 API 方法</name>
  <files>log-analyzer_flutter/lib/shared/services/api_service.dart</files>
  <action>
在 ApiService 中添加压缩包导入方法：
1. 添加 importArchive(workspaceId, archivePath) 方法
2. 添加 listArchiveContents(archivePath) 方法 - 列出压缩包内容
3. 添加 importArchiveFiles(workspaceId, archivePath, selectedFiles) 方法 - 选择性导入
4. 定义 ArchiveType 枚举（zip, tar, gz, rar, sevenz）
5. 定义 ArchiveEntry 数据类（name, path, size, isDirectory）
6. 定义 ArchiveContents 数据类（type, entries）
7. 映射到 Rust 后端命令（import_archive, list_archive）
  </action>
  <verify>
    <automated>flutter test test/shared/services/search_query_builder_test.dart 2>&1 | head -5</automated>
  </verify>
  <done>ApiService 包含压缩包导入相关方法</done>
</task>

<task type="auto">
  <name>Task 2: 创建压缩包导入对话框</name>
  <files>log-analyzer_flutter/lib/shared/widgets/archive_import_dialog.dart</files>
  <action>
创建压缩包导入对话框组件：
1. 显示压缩包内容列表（TreeView 或 ListView）
2. 显示文件大小和类型图标
3. 支持全选/取消全选
4. 支持多选要导入的文件
5. 显示预估解压后大小
6. 导入按钮和取消按钮
7. 导入进度集成（复用 ImportProgressDialog）
8. 完成后显示摘要报告
  </action>
  <verify>
    <automated>flutter test test/widgets/import_progress_dialog_test.dart</automated>
  </verify>
  <done>用户可以预览压缩包内容并选择要导入的文件</done>
</task>

<task type="auto">
  <name>Task 3: 集成压缩包检测到拖放区</name>
  <files>log-analyzer_flutter/lib/shared/widgets/drop_zone.dart</files>
  <action>
增强拖放区域支持压缩包检测：
1. 在 onFilesDropped 回调中检测文件扩展名
2. 识别压缩包格式：.zip, .tar, .tar.gz/.tgz, .rar, .7z
3. 对于压缩包，显示 ArchiveImportDialog 预览
4. 对于文件夹，直接调用导入
5. 检测到混合内容时，分类处理
6. 密码保护提示（如果后端返回需要密码）
  </action>
  <verify>
    <automated>flutter test test/widgets/import_progress_dialog_test.dart</automated>
  </verify>
  <done>拖放区域可以自动识别并处理压缩包</done>
</task>

<task type="auto">
  <name>Task 4: 添加按钮导入压缩包支持</name>
  <files>log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart</files>
  <action>
在导入按钮中支持压缩包：
1. 修改 file_picker 支持多选（allowMultiple: true）
2. 添加文件类型过滤：.zip, .tar, .gz, .rar, .7z
3. 检测到压缩包时显示 ArchiveImportDialog
4. 检测到文件夹时直接导入
5. 检测到混合内容时分类处理
6. 添加重复文件检测和用户选择
  </action>
  <verify>
    <automated>flutter test test/workspace/workspace_provider_test.dart</automated>
  </verify>
  <done>导入按钮支持选择压缩包文件</done>
</task>

</tasks>

<verification>
1. 启动 Flutter 应用
2. 拖放一个 ZIP 文件到工作区页面
3. 验证弹出 ArchiveImportDialog 预览内容
4. 选择文件，点击导入，验证进度显示
5. 验证其他格式（TAR/GZ/RAR/7Z）同样工作
</verification>

<success_criteria>
- [x] 支持 ZIP 压缩包导入
- [x] 支持 TAR 压缩包导入
- [x] 支持 GZIP 压缩包导入
- [x] 支持 RAR 压缩包导入
- [x] 支持 7Z 压缩包导入
- [x] 压缩包内容预览功能
- [x] 选择性解压功能
</success_criteria>

<output>
完成后创建 `.planning/phases/02-workspace-import/02-03-SUMMARY.md`
</output>
