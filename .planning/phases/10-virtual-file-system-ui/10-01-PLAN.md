---
phase: 10-virtual-file-system-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/virtual_file_tree/presentation/virtual_file_tree_page.dart
  - log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_sidebar.dart
  - log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart
  - log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_node.dart
  - log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_type_icon.dart
  - log-analyzer_flutter/lib/features/virtual_file_tree/providers/file_tree_ui_provider.dart
autonomous: true
requirements:
  - VFS-01
  - VFS-04

must_haves:
  truths:
    - 用户可以在侧边栏查看工作区的虚拟文件树结构
    - 文件树使用不同图标区分文件和目录类型
    - 文件树节点显示文件名（悬停显示完整路径 tooltip）
    - 侧边栏宽度可拖动调整，最小 200px，最大 500px
    - 支持完整键盘导航（上下箭头导航、左右箭头折叠/展开、回车打开预览）
  artifacts:
    - path: "log-analyzer_flutter/lib/features/virtual_file_tree/presentation/virtual_file_tree_page.dart"
      provides: "页面入口，整合文件树侧边栏和标签页"
      exports: ["VirtualFileTreePage"]
    - path: "log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_sidebar.dart"
      provides: "左侧边栏组件，支持可调宽度"
      exports: ["FileTreeSidebar"]
    - path: "log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart"
      provides: "树形视图组件，使用 TreeSliver"
      exports: ["FileTreeView"]
    - path: "log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_node.dart"
      provides: "单个文件树节点组件"
      exports: ["FileTreeNode"]
    - path: "log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_type_icon.dart"
      provides: "文件类型图标映射"
      exports: ["getFileIcon", "directoryIcon"]
  key_links:
    - from: "file_tree_view.dart"
      to: "virtual_file_tree_provider.dart"
      via: "Riverpod watch"
      pattern: "ref.watch(virtualFileTreeProvider"
---

<objective>
实现虚拟文件树 UI 的核心组件：树形视图、文件/目录图标区分、侧边栏布局。

Purpose: 为用户提供类似 VS Code 的文件树浏览体验，支持查看工作区虚拟文件结构和文件类型区分。

Output: 创建 virtual_file_tree 特性模块，包含页面、侧边栏、树形视图、节点组件、图标映射。
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-virtual-file-system-ui/10-CONTEXT.md
@.planning/phases/10-virtual-file-system-ui/10-RESEARCH.md

# 现有代码参考

## VirtualFileTreeProvider API (来自 virtual_file_tree_provider.dart:112-348)
```dart
@riverpod
class VirtualFileTree extends _$VirtualFileTree {
  @override
  Future<List<VirtualTreeNode>> build(String workspaceId) async {...}

  /// 懒加载子节点
  Future<List<VirtualTreeNode>> loadChildren(String parentPath) async {...}

  /// 读取文件内容
  Future<FileContentResponse?> readFileByHash(String hash) async {...}
}
```

## VirtualTreeNode 模型 (来自 virtual_file_tree_provider.dart:24-46)
```dart
@freezed
sealed class VirtualTreeNode with _$VirtualTreeNode {
  const factory VirtualTreeNode.file({
    required String name,
    required String path,
    required String hash,
    required int size,
    String? mimeType,
  }) = VirtualTreeNodeFile;

  const factory VirtualTreeNode.archive({
    required String name,
    required String path,
    required String hash,
    required String archiveType,
    List<VirtualTreeNode> children,
  }) = VirtualTreeNodeArchive;
}
```

## 扩展方法 (来自 virtual_file_tree_provider.dart:353-401)
```dart
extension VirtualTreeNodeExtension on VirtualTreeNode {
  String get nodeName;
  String get nodePath;
  String get nodeHash;
  VirtualTreeNodeType get nodeType;
  bool get isArchive;
  bool get isFile;
  List<VirtualTreeNode> get children;
  bool get hasChildren;
  bool get needsLazyLoad;
}
```

## ErrorView 组件 (来自 error_view.dart)
```dart
class ErrorView extends StatelessWidget {
  final AppException exception;
  final VoidCallback? onRetry;
  final String? message;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 VirtualFileTreeFeature 目录结构和类型图标映射</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_type_icon.dart</files>
  <action>
1. 在 `log-analyzer_flutter/lib/features/virtual_file_tree/` 下创建目录结构:
   - presentation/widgets/
   - presentation/controllers/
   - providers/

2. 创建文件类型图标映射 `file_type_icon.dart`:
   - 使用 lucide_icons_flutter 包
   - 实现 `getFileIcon(String fileName)` 函数，根据扩展名返回对应图标
   - 支持: log, txt, json, xml, zip, tar, gz, rar, 7z, pdf, jpg, png, gif 等
   - 默认返回 LucideIcons.file
   - 定义 `directoryIcon` 和 `directoryOpenIcon` 常量

3. 确保在 pubspec.yaml 中添加 shimmer 依赖（如果尚未添加）
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/widgets/file_type_icon.dart 无错误</verify>
  <done>文件类型图标映射创建完成，支持常见文件扩展名到图标的映射</done>
</task>

<task type="auto">
  <name>Task 2: 创建 FileTreeNode 组件 - 单个节点展示</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_node.dart</files>
  <action>
1. 创建 FileTreeNode widget:
   - 接收 `VirtualTreeNode` 节点数据
   - 接收 `bool isExpanded` 参数表示展开状态
   - 接收 `bool isSelected` 参数表示选中状态
   - 接收 `VoidCallback onTap` 点击回调
   - 接收 `VoidCallback onExpand` 展开/折叠回调

2. 节点布局:
   - 左侧: 展开/折叠箭头图标（仅目录显示）
   - 中间: 文件类型图标（使用 file_type_icon.dart 的 getFileIcon）
   - 右侧: 文件名（使用 Tooltip 包裹，hover 显示完整 path）

3. 样式:
   - 行高: 28px（紧凑模式）
   - 选中背景: Theme 主题色
   - Hover 背景: 浅色覆盖
   - 使用 ListTile 风格或自定义 Row
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/widgets/file_tree_node.dart 无错误</verify>
  <done>FileTreeNode 组件创建完成，支持文件/目录图标、展开箭头、选中状态、tooltip</done>
</task>

<task type="auto">
  <name>Task 3: 创建 FileTreeView 组件 - TreeSliver 树形视图</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart</files>
  <action>
1. 创建 FileTreeView widget:
   - 使用 Flutter 内置 TreeSliver（需要 Flutter 3.24.0+）
   - 或使用 CustomScrollView + SliverList 实现树形

2. 数据流:
   - 接收 `List<VirtualTreeNode>` 根节点列表
   - 接收展开状态 Map（path -> isExpanded）
   - 接收 `Function(VirtualTreeNode) onNodeTap` 点击回调
   - 接收 `Function(String parentPath) onExpand` 展开回调

3. 使用 TreeSliverController 管理展开/折叠状态

4. 渲染逻辑:
   - 递归渲染子节点
   - 使用 FileTreeNode 组件渲染每个节点
   - 展开状态控制子节点显示/隐藏
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart 无错误</verify>
  <done>FileTreeView 组件创建完成，支持树形展示、展开/折叠状态管理</done>
</task>

<task type="auto">
  <name>Task 4: 创建 FileTreeSidebar 组件 - 可调宽度侧边栏</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_sidebar.dart</files>
  <action>
1. 创建 FileTreeSidebar widget:
   - 使用 LayoutBuilder 获取可用宽度
   - 实现可拖动调整宽度: 右侧边缘添加 4px 宽的拖动区域

2. 拖动行为:
   - 最小宽度: 200px
   - 最大宽度: 500px
   - 使用 GestureDetector 监听 onHorizontalDragUpdate
   - 拖动时显示宽度指示器

3. 内部布局:
   - 顶部: 标题栏 "文件树" + 折叠按钮
   - 主体: FileTreeView 组件
   - 使用垂直分割线指示拖动区域

4. 宽度状态管理:
   - 在 file_tree_ui_provider.dart 中添加 sidebarWidth 状态
   - 使用 SharedPreferences 持久化用户偏好
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/widgets/file_tree_sidebar.dart 无错误</verify>
  <done>FileTreeSidebar 组件创建完成，支持拖动调整宽度 (200-500px)</done>
</task>

<task type="auto">
  <name>Task 5: 实现键盘导航功能 - 上下左右箭头 + 回车</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart</files>
  <action>
1. 在 FileTreeView 中添加 KeyboardListener 或 Focus 部件:
   - 使用 Focus widget 包裹树形视图
   - 设置 autofocus: true

2. 实现键盘事件处理:
   - 上箭头 (ArrowUp): 选中上一个节点
   - 下箭头 (ArrowDown): 选中下一个节点
   - 左箭头 (ArrowLeft): 折叠当前目录（如果展开）
   - 右箭头 (ArrowRight): 展开当前目录（如果是目录且未展开）
   - 回车 (Enter): 预览/打开选中节点

3. 焦点管理:
   - 维护当前焦点节点路径
   - 确保焦点移动时滚动到可见区域
   - 首次加载时自动选中第一个节点

4. 与 UI Provider 集成:
   - 调用 fileTreeUIProvider 的选中方法
   - 使用展开状态方法控制折叠/展开
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/widgets/file_tree_view.dart 无错误</verify>
  <done>键盘导航功能实现完成，支持上下箭头导航、左右箭头折叠/展开、回车打开预览</done>
</task>

<task type="auto">
  <name>Task 6: 创建 VirtualFileTreePage 入口页面</name>
  <files>log-analyzer_flutter/lib/features/virtual_file_tree/presentation/virtual_file_tree_page.dart</files>
  <action>
1. 创建 VirtualFileTreePage 页面组件:
   - 使用 Riverpod 获取工作区 ID
   - 监听 VirtualFileTreeProvider 获取文件树数据

2. 页面布局:
   - 左侧: FileTreeSidebar（可调宽度侧边栏）
   - 右侧: 主内容区（标签页切换）
   - 使用 Row 布局

3. 标签页切换:
   - 标签1: "日志列表" - 现有搜索结果
   - 标签2: "文件树" - 虚拟文件系统
   - 使用 TabBar + TabBarView 实现

4. 状态处理:
   - 空状态: 显示 VirtualFileTreeEmptyState
   - 加载状态: 显示 FileTreeLoadingSkeleton
   - 错误状态: 显示 ErrorView 并提供重试

5. 与现有页面整合:
   - 考虑作为 TabPage 的一个标签页
   - 或作为独立路由 /virtual-file-tree
  </action>
  <verify>flutter analyze lib/features/virtual_file_tree/presentation/virtual_file_tree_page.dart 无错误</verify>
  <done>VirtualFileTreePage 入口页面创建完成，整合侧边栏和标签页</done>
</task>

</tasks>

<verification>
- [ ] 用户可以在侧边栏查看工作区的虚拟文件树结构
- [ ] 文件树使用不同图标区分文件和目录类型
- [ ] 文件树节点显示文件名，悬停显示完整路径
</verification>

<success_criteria>
VirtualFileTreePage 组件能够渲染文件树，使用 TreeSliver 展示层级结构，文件/目录使用正确图标区分。
</success_criteria>

<output>
After completion, create `.planning/phases/10-virtual-file-system-ui/10-01-SUMMARY.md`
</output>
