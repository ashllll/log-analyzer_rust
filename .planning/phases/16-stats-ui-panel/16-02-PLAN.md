---
phase: 16-stats-ui-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/search_page.dart
  - log-analyzer_flutter/lib/features/search/presentation/widgets/log_level_stats_panel.dart
autonomous: true
requirements:
  - STATS-04
must_haves:
  truths:
    - 用户可以在搜索页面看到日志级别统计面板
    - 点击级别可快速筛选对应日志
  artifacts:
    - path: log-analyzer_flutter/lib/features/search/presentation/search_page.dart
      provides: 集成日志级别统计面板到搜索页面
      contains: LogLevelStatsPanel
  key_links:
    - from: search_page.dart
      to: log_level_stats_panel.dart
      via: import
      pattern: "import.*log_level_stats_panel"
    - from: search_page.dart
      to: search_provider.dart
      via: applyFilters
      pattern: "applyFilters.*FilterOptions"
---

<objective>
将日志级别统计面板集成到搜索页面，实现按级别快速筛选功能

Purpose: 用户可以在搜索页面查看日志级别统计，并通过点击级别快速筛选
Output: 搜索页面集成 LogLevelStatsPanel
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@log-analyzer_flutter/lib/features/search/presentation/search_page.dart
@log-analyzer_flutter/lib/features/search/presentation/widgets/log_level_stats_panel.dart (from Plan 01)

# 现有 SearchPage 结构:
- 使用 Riverpod ConsumerStatefulWidget
- 已有 applyFilters(FilterOptions) 方法
- FilterOptions 包含 levels: List<String> 参数

# LogLevelStatsPanel 预期接口:
class LogLevelStatsPanel extends ConsumerWidget {
  final String workspaceId;
  final Function(List<String>)? onLevelFilter;
}
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 SearchPage 中添加导入和状态</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
1. 在 search_page.dart 文件顶部添加 LogLevelStatsPanel 导入:
   import '../widgets/log_level_stats_panel.dart';

2. 找到合适的位置添加统计面板:
   - 方案 A: 在搜索结果列表上方添加可折叠面板
   - 方案 B: 在侧边栏添加统计面板
   - 选择方案 A (更直观，用户一眼能看到)

3. 添加展开/折叠状态管理 (如果需要可折叠)
  </action>
  <verify>flutter analyze lib/features/search/presentation/search_page.dart</verify>
  <done>导入已添加，准备好集成 LogLevelStatsPanel</done>
</task>

<task type="auto">
  <name>Task 2: 集成 LogLevelStatsPanel 到搜索页面</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
1. 在搜索页面布局中添加 LogLevelStatsPanel:
   - 位置: 搜索输入框下方，搜索结果列表上方
   - 使用 Container 包裹，设置合适的高度和边框

2. 实现 onLevelFilter 回调:
   ```dart
   void _onLevelFilter(List<String> levels) {
     // 调用现有的 applyFilters 方法
     applyFilters(FilterOptions(
       timeRange: _currentTimeRange,
       levels: levels,
       filePattern: _currentFilePattern,
     ));
   }
   ```

3. 传递 workspaceId 参数:
   - 从当前工作区状态获取 workspaceId
   - 传递给 LogLevelStatsPanel

4. 调整布局样式:
   - 高度: 约 200-300px
   - 边距: 16px
   - 背景: AppColors.bgCard
   - 边框: 底部 Border (参考 SearchStatsPanel)
  </action>
  <verify>flutter analyze lib/features/search/presentation/search_page.dart</verify>
  <done>LogLevelStatsPanel 已集成到搜索页面，点击级别可触发筛选</done>
</task>

</tasks>

<verification>
- [ ] 搜索页面显示日志级别统计面板
- [ ] 点击级别卡片可筛选对应日志
- [ ] 点击饼图扇区可筛选对应日志
- [ ] 筛选后搜索结果正确过滤
</verification>

<success_criteria>
用户可以在搜索页面看到日志级别统计面板，点击级别可快速筛选对应日志
</success_criteria>

<output>
After completion, create .planning/phases/16-stats-ui-panel/16-02-SUMMARY.md
</output>
