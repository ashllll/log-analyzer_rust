## 步骤1: 删除 search.rs 中的死代码函数

**任务**: 删除 `search_single_file` 函数及其注释 (line 629-694)

**修改内容**:
- 移除 `#[allow(dead_code)]` 标记的函数
- 移除相关的注释说明
- 不影响其他功能

**验证方法**:
- 运行 `cargo clippy` 检查编译
- 运行 `cargo test` 确保测试通过

**影响范围**:
- 仅影响一个未使用的函数
- 不影响任何现有功能