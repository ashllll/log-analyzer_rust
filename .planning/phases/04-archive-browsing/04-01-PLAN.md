---
phase: 04-archive-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer/src-tauri/src/archive/archive_handler.rs
  - log-analyzer/src-tauri/src/archive/zip_handler.rs
  - log-analyzer/src-tauri/src/archive/tar_handler.rs
  - log-analyzer/src-tauri/src/archive/gz_handler.rs
  - log-analyzer/src-tauri/src/archive/rar_handler.rs
  - log-analyzer/src-tauri/src/archive/sevenz_handler.rs
  - log-analyzer/src-tauri/src/archive/mod.rs
  - log-analyzer/src-tauri/src/commands/archive.rs
  - log-analyzer/src-tauri/src/commands/mod.rs
  - log-analyzer/src-tauri/src/lib.rs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "用户可以浏览压缩包内的文件列表"
    - "用户可以预览压缩包内的文本文件内容"
    - "用户可以在压缩包内搜索关键词"
  artifacts:
    - path: "log-analyzer/src-tauri/src/archive/archive_handler.rs"
      provides: "ArchiveEntry 数据结构 + ArchiveHandler trait 扩展"
      contains: "list_contents, read_file 方法"
    - path: "log-analyzer/src-tauri/src/commands/archive.rs"
      provides: "Tauri 命令接口"
      exports: ["list_archive_contents", "read_archive_file"]
    - path: "log-analyzer_flutter/lib/features/archive_browsing/presentation/pages/archive_browser_page.dart"
      provides: "压缩包浏览主页面"
  key_links:
    - from: "archive_handler.rs"
      to: "zip_handler.rs, tar_handler.rs, gz_handler.rs, rar_handler.rs, sevenz_handler.rs"
      via: "trait 方法实现"
      pattern: "impl ArchiveHandler for.*Handler"
    - from: "commands/archive.rs"
      to: "archive_handler.rs"
      via: "handler.list_contents(), handler.read_file()"
      pattern: "find_handler.*list_contents"
---

<objective>
实现压缩包内容浏览后端功能：列出压缩包文件列表、读取单个文件内容、创建 Tauri 命令接口。

Purpose: 为前端 Flutter UI 提供后端支持，实现 ARCH-01（浏览文件列表）、ARCH-02（预览文本文件）、ARCH-03（压缩包内搜索）三个需求。

Output:
- 扩展后的 ArchiveHandler trait（含 list_contents 和 read_file 方法）
- 各格式处理器实现（ZIP/TAR/GZ/RAR/7Z）
- 新的 Tauri 命令（list_archive_contents, read_archive_file）
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-archive-browsing/04-CONTEXT.md
@.planning/phases/04-archive-browsing/04-RESEARCH.md
@log-analyzer/src-tauri/src/archive/archive_handler.rs
@log-analyzer/src-tauri/src/archive/zip_handler.rs

## User Decisions (Locked)
- 树形视图展示嵌套目录结构
- Split Pane 布局（左侧列表，右侧预览）
- 单击文件立即预览
- 预览支持关键词高亮
- 实时搜索模式
- 支持所有主流格式: ZIP/TAR/GZ/RAR/7Z
- 大文件截断并提示
- 空压缩包或无法预览时显示友好提示

## 现有 ArchiveHandler Trait
```rust
#[async_trait]
pub trait ArchiveHandler: Send + Sync {
    fn can_handle(&self, path: &Path) async fn extract_with -> bool;
   _limits(&self, source: &Path, target_dir: &Path, ...) -> Result<ExtractionSummary>;
    fn file_extensions(&self) -> Vec<&str>;
}
```

## 需要扩展的方法
```rust
/// 列出压缩包内容（不解压）
fn list_contents(&self, path: &Path) -> Result<Vec<ArchiveEntry>>;

/// 读取单个文件内容
fn read_file(&self, path: &Path, file_name: &str) -> Result<String>;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ArchiveHandler trait 添加新方法</name>
  <files>log-analyzer/src-tauri/src/archive/archive_handler.rs</files>
  <action>
1. 在 archive_handler.rs 中添加 `ArchiveEntry` 数据结构：
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ArchiveEntry {
    pub name: String,           // 文件/目录名称
    pub path: String,           // 完整路径
    pub is_dir: bool,           // 是否为目录
    pub size: u64,             // 文件大小（字节）
    pub compressed_size: u64,   // 压缩后大小
}
```

2. 扩展 `ArchiveHandler` trait，添加两个新方法：
```rust
/// 列出压缩包内容（不解压）
fn list_contents(&self, path: &Path) -> Result<Vec<ArchiveEntry>>;

/// 读取单个文件内容（带大小限制，防止内存爆炸）
fn read_file(&self, path: &Path, file_name: &str) -> Result<String>;
```

注意：read_file 方法需要实现大文件截断逻辑，默认阈值 10MB，超过后返回截断内容并附带提示信息。
  </action>
  <verify>cargo check --lib -p log-analyzer</verify>
  <done>ArchiveHandler trait 扩展完成，包含 list_contents 和 read_file 方法签名</done>
</task>

<task type="auto">
  <name>Task 2: 实现 ZIP 格式的 list_contents 和 read_file</name>
  <files>log-analyzer/src-tauri/src/archive/zip_handler.rs</files>
  <action>
1. 为 ZipHandler 实现 `list_contents` 方法：
```rust
fn list_contents(&self, path: &Path) -> Result<Vec<ArchiveEntry>> {
    let file = std::fs::File::open(path)?;
    let mut archive = ZipArchive::new(file)?;
    let mut entries = Vec::new();

    for i in 0..archive.len() {
        let file = archive.by_index(i)?;
        entries.push(ArchiveEntry {
            name: PathBuf::from(file.name())
                .file_name()
                .map(|s| s.to_string_lossy().to_string())
                .unwrap_or_default(),
            path: file.name().to_string(),
            is_dir: file.is_dir(),
            size: file.size(),
            compressed_size: file.compressed_size(),
        });
    }
    Ok(entries)
}
```

2. 实现 `read_file` 方法：
```rust
fn read_file(&self, path: &Path, file_name: &str) -> Result<String> {
    let file = std::fs::File::open(path)?;
    let mut archive = ZipArchive::new(file)?;
    let mut zip_file = archive.by_name(file_name)?;

    // 大小限制：10MB
    const MAX_SIZE: u64 = 10 * 1024 * 1024;
    let size = zip_file.size();

    if size > MAX_SIZE {
        // 大文件截断读取
        let mut buffer = vec![0u8; MAX_SIZE as usize];
        let bytes_read = zip_file.read(&mut buffer)?;
        let mut content = String::from_utf8_lossy(&buffer[..bytes_read]).to_string();
        content.push_str(&format!("\n\n[文件过大，已截断显示. 完整大小: {} bytes]", size));
        Ok(content)
    } else {
        let mut contents = String::new();
        zip_file.read_to_string(&mut contents)?;
        Ok(contents)
    }
}
```
  </action>
  <verify>cargo test zip_handler --lib -- --nocapture 2>/dev/null || cargo check --lib -p log-analyzer</verify>
  <done>ZipHandler 实现完成，支持列出内容和读取文件</done>
</task>

<task type="auto">
  <name>Task 3: 实现 TAR/GZ/RAR/7Z 格式的 list_contents 和 read_file</name>
  <files>
    log-analyzer/src-tauri/src/archive/tar_handler.rs
    log-analyzer/src-tauri/src/archive/gz_handler.rs
    log-analyzer/src-tauri/src/archive/rar_handler.rs
    log-analyzer/src-tauri/src/archive/sevenz_handler.rs
  </files>
  <action>
为 TarHandler、GzHandler、RarHandler、SevenzHandler 各自实现 list_contents 和 read_file 方法。

参考 ZipHandler 实现，确保：
1. list_contents 返回所有文件/目录条目的 ArchiveEntry 列表
2. read_file 支持 10MB 大小截断
3. 处理编码问题（使用 encoding_rs 尝试 UTF-8/GBK）
4. 空文件或无法预览时返回友好错误信息

注意：纯 GZ 格式（gz_handler）只包含单个文件，list_contents 返回一个 entry，read_file 读取该文件。
  </action>
  <verify>cargo check --lib -p log-analyzer</verify>
  <done>所有格式处理器实现完成</done>
</task>

<task type="auto">
  <name>Task 4: 创建 ArchiveManager 辅助方法并更新 mod.rs</name>
  <files>
    log-analyzer/src-tauri/src/archive/mod.rs
  </files>
  <action>
1. 在 ArchiveManager（或模块级别）添加 find_handler 辅助函数：
```rust
pub fn find_handler(path: &Path) -> Option<Box<dyn ArchiveHandler>> {
    // 遍历所有已注册的 handler，返回第一个 can_handle 返回 true 的
}
```

2. 在 mod.rs 中导出新的类型和方法：
```rust
pub use archive_handler::{ArchiveEntry, ArchiveHandler};
pub mod archive_commands; // 如果需要模块化
```

3. 确保所有 handler 模块在 mod.rs 中正确导入
</action>
  <verify>cargo check --lib -p log-analyzer</verify>
  <done>模块导出和辅助函数完成</done>
</task>

<task type="auto">
  <name>Task 5: 创建 Tauri 命令 archive.rs</name>
  <files>
    log-analyzer/src-tauri/src/commands/archive.rs
    log-analyzer/src-tauri/src/commands/mod.rs
    log-analyzer/src-tauri/src/lib.rs
  </files>
  <action>
1. 创建 commands/archive.rs，添加两个 Tauri 命令：

```rust
use serde::{Deserialize, Serialize};
use crate::archive::{ArchiveEntry, find_handler};

#[derive(Debug, Serialize, Deserialize)]
pub struct ArchiveContentResult {
    pub entries: Vec<ArchiveEntry>,
    pub total_count: usize,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ArchiveFileResult {
    pub content: String,
    pub size: usize,
    pub truncated: bool,
}

/// 列出压缩包内容
#[tauri::command]
pub async fn list_archive_contents(archive_path: String) -> Result<ArchiveContentResult, String> {
    let path = std::path::Path::new(&archive_path);
    let handler = find_handler(path).ok_or("Unsupported archive format")?;

    let entries = handler.list_contents(path).map_err(|e| e.to_string())?;

    Ok(ArchiveContentResult {
        total_count: entries.len(),
        entries,
    })
}

/// 读取压缩包内单个文件
#[tauri::command]
pub async fn read_archive_file(archive_path: String, file_name: String) -> Result<ArchiveFileResult, String> {
    let path = std::path::Path::new(&archive_path);
    let handler = find_handler(path).ok_or("Unsupported archive format")?;

    let content = handler.read_file(path, &file_name).map_err(|e| e.to_string())?;
    let truncated = content.contains("已截断显示");
    let size = content.len();

    Ok(ArchiveFileResult {
        content,
        size,
        truncated,
    })
}
```

2. 在 commands/mod.rs 中添加：
```rust
pub mod archive;
```

3. 在 lib.rs 的 invoke_handler 中注册命令：
```rust
.invoke_handler(tauri::generate_handler![
    // ... 现有命令
    commands::archive::list_archive_contents,
    commands::archive::read_archive_file,
])
```
  </action>
  <verify>cargo check --lib -p log-analyzer && cargo build --lib -p log-analyzer</verify>
  <done>Tauri 命令创建并注册完成</done>
</task>

</tasks>

<verification>
- [ ] ArchiveHandler trait 扩展完成
- [ ] 所有格式处理器实现 list_contents 和 read_file
- [ ] Tauri 命令创建并可调用
- [ ] cargo check 通过
- [ ] ARCH-01/02/03 后端支持就绪
</verification>

<success_criteria>
1. list_archive_contents 命令可列出任意支持格式的压缩包内容
2. read_archive_file 命令可读取压缩包内文本文件（10MB 截断）
3. 大文件自动截断并提示用户
4. 空压缩包或不支持格式返回友好错误
</success_criteria>

<output>
完成后创建 `.planning/phases/04-archive-browsing/04-01-SUMMARY.md`
</output>
