---
phase: 09-advanced-search-ui
plan: 04
type: execute
wave: 2
depends_on:
  - 09-03
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/widgets/search_history_dropdown.dart
  - log-analyzer_flutter/lib/features/search/presentation/search_page.dart
autonomous: true
requirements:
  - HIST-04
  - HIST-05
must_haves:
  truths:
    - "用户可以删除单条历史记录"
    - "用户可以一键清空所有搜索历史"
    - "清空前显示确认对话框"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/search_history_dropdown.dart"
      provides: "增强的历史下拉组件"
    - path: "log-analyzer_flutter/lib/features/search/presentation/search_page.dart"
      provides: "清空历史功能"
  key_links:
    - from: "search_history_dropdown.dart"
      to: "SearchHistoryProvider.deleteSearchHistory()"
      via: "删除单条"
      pattern: "deleteSearchHistory"
    - from: "search_page.dart"
      to: "SearchHistoryProvider.clearSearchHistory()"
      via: "清空全部"
      pattern: "clearSearchHistory"
---

<objective>
实现搜索历史管理功能，支持删除单条和清空全部历史，清空前显示确认对话框。

Purpose: 让用户能够管理搜索历史，保护用户数据隐私。
Output: 增强的 SearchHistoryDropdown，清空确认对话框
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-search-ui/09-RESEARCH.md
@.planning/phases/09-advanced-search-ui/09-03-PLAN.md

<interfaces>
<!-- Key types from existing providers -->

From lib/shared/providers/search_history_provider.dart:
```dart
@riverpod
class SearchHistory extends _$SearchHistory {
  Future<void> deleteSearchHistory(String query)
  Future<void> clearSearchHistory()
}
```

From RESEARCH 清空历史代码示例:
```dart
Future<void> _clearAllHistory() async {
  final confirmed = await showDialog<bool>(
    context: context,
    builder: (context) => AlertDialog(
      title: const Text('确认清空'),
      content: const Text('确定要清空所有搜索历史吗？此操作不可恢复。'),
      actions: [
        TextButton(onPressed: () => Navigator.pop(context, false), child: const Text('取消')),
        ElevatedButton(
          onPressed: () => Navigator.pop(context, true),
          style: ElevatedButton.styleFrom(backgroundColor: AppColors.error),
          child: const Text('确认清空'),
        ),
      ],
    ),
  );

  if (confirmed == true) {
    await ref.read(searchHistoryProvider(workspaceId).notifier).clearSearchHistory();
  }
}
```
</interfaces>

**已有基础:**
- 09-03 已实现 SearchHistoryDropdown 基本功能
- SearchHistoryProvider 已有 clearSearchHistory 方法
- 乐观更新已实现
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 SearchHistoryDropdown 支持删除单条</name>
  <files>
    log-analyzer_flutter/lib/features/search/presentation/widgets/search_history_dropdown.dart
  </files>
  <action>
增强 SearchHistoryDropdown 组件，添加删除单条历史功能。

1. **修改列表项布局:**
   - 使用 ListTile 或自定义 Row
   - 左侧: 历史图标 + 查询文本
   - 中间: 结果数量 (灰色小字)
   - 右侧: 删除按钮 (IconButton)

2. **删除交互:**
   - 点击删除按钮触发 onDelete 回调
   - 阻止事件冒泡 (避免同时触发 onSelect)
   - 使用 `GestureDetector` 或 `InkWell` 包裹删除按钮

3. **关键代码:**
```dart
PopupMenuItem<String>(
  value: item.query,
  child: Row(
    children: [
      const Icon(Icons.history, size: 16),
      const SizedBox(width: 8),
      Expanded(child: Text(item.query)),
      Text('${item.resultCount} 条', style: textTheme.bodySmall),
      IconButton(
        icon: const Icon(Icons.close, size: 16),
        onPressed: () {
          Navigator.pop(context); // 关闭下拉
          widget.onDelete?.call(item.query);
        },
      ),
    ],
  ),
)
```

4. **样式优化:**
   - 删除按钮使用浅灰色，hover 时变红
   - 列表项之间有分隔线
   - 长查询文本截断显示

5. **注意事项:**
   - 确保删除按钮点击区域足够大 (48px)
   - 使用 IconButton 的 padding 属性调整
  </action>
  <verify>
```bash
cd log-analyzer_flutter && flutter analyze lib/features/search/presentation/widgets/search_history_dropdown.dart
```
  </verify>
  <done>
- SearchHistoryDropdown 支持删除单条
- 删除按钮样式正确
- 事件冒泡正确阻止
- 无 analyzer 错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加清空全部历史功能</name>
  <files>
    log-analyzer_flutter/lib/features/search/presentation/widgets/search_history_dropdown.dart
    log-analyzer_flutter/lib/features/search/presentation/search_page.dart
  </files>
  <action>
添加清空所有搜索历史功能，包括确认对话框。

1. **修改 SearchHistoryDropdown:**
   - 在下拉列表底部添加 "清空全部" 按钮
   - 点击时触发 onClearAll 回调
   - 历史为空时禁用此按钮

2. **布局结构:**
```
[历史记录 1]                    [x]
[历史记录 2]                    [x]
...
─────────────────────────────
[清空全部历史]
```

3. **关键代码 (Dropdown 底部):**
```dart
Column(
  mainAxisSize: MainAxisSize.min,
  children: [
    // 历史列表
    ...history.map((item) => _buildHistoryItem(item)),

    // 分隔线
    if (history.isNotEmpty) const Divider(),

    // 清空按钮
    if (history.isNotEmpty)
      TextButton.icon(
        icon: const Icon(Icons.delete_sweep, size: 18),
        label: const Text('清空全部历史'),
        style: TextButton.styleFrom(foregroundColor: AppColors.error),
        onPressed: widget.onClearAll,
      ),
  ],
)
```

4. **在 SearchPage 添加清空确认对话框:**
   - 创建 `_showClearHistoryConfirmation()` 方法
   - 使用 AlertDialog
   - 确认按钮使用红色 (危险操作)
   - 确认后调用 `clearSearchHistory()`

5. **确认对话框实现:**
```dart
Future<void> _showClearHistoryConfirmation(String workspaceId) async {
  final confirmed = await showDialog<bool>(
    context: context,
    builder: (context) => AlertDialog(
      title: const Text('确认清空'),
      content: const Text('确定要清空所有搜索历史吗？此操作不可恢复。'),
      actions: [
        TextButton(
          onPressed: () => Navigator.pop(context, false),
          child: const Text('取消'),
        ),
        ElevatedButton(
          onPressed: () => Navigator.pop(context, true),
          style: ElevatedButton.styleFrom(
            backgroundColor: AppColors.error,
          ),
          child: const Text('确认清空'),
        ),
      ],
    ),
  );

  if (confirmed == true) {
    await ref.read(searchHistoryProvider(workspaceId).notifier)
        .clearSearchHistory();
    ref.read(appStateProvider.notifier).addToast(
      ToastType.success,
      '搜索历史已清空',
    );
  }
}
```

6. **连接 onClearAll 回调:**
   - 在 SearchPage 中传递 onClearAll 参数
   - 调用 _showClearHistoryConfirmation
  </action>
  <verify>
```bash
cd log-analyzer_flutter && flutter analyze lib/features/search/presentation/widgets/search_history_dropdown.dart lib/features/search/presentation/search_page.dart
```
  </verify>
  <done>
- SearchHistoryDropdown 底部显示清空按钮
- 点击清空显示确认对话框
- 确认后清空所有历史
- 乐观更新立即生效
- Toast 提示成功
  </done>
</task>

<task type="auto">
  <name>Task 3: 最终集成测试</name>
  <files>
    log-analyzer_flutter/lib/features/search/presentation/search_page.dart
  </files>
  <action>
确保所有搜索历史功能正确集成并协同工作。

1. **验证完整流程:**
   - 执行搜索 -> 自动保存历史
   - 打开历史下拉 -> 显示历史列表
   - 点击历史条目 -> 填充搜索框
   - 点击删除按钮 -> 删除单条
   - 点击清空全部 -> 确认对话框 -> 清空

2. **检查边界情况:**
   - 空历史列表显示提示
   - 长查询文本截断
   - 结果数量显示正确
   - 删除后列表立即更新

3. **添加 widget key:**
   - 为关键组件添加 Key 便于测试
   - SearchHistoryDropdown 添加 key 参数

4. **代码整理:**
   - 移除调试代码
   - 添加必要的注释
   - 确保中文注释 (符合 CLAUDE.md)
  </action>
  <verify>
```bash
cd log-analyzer_flutter && flutter analyze lib/features/search/
```
  </verify>
  <done>
- 所有搜索历史功能正常工作
- 边界情况处理完善
- 代码整洁无警告
- 关键组件有 Key
  </done>
</task>

</tasks>

<verification>
## 功能验证

1. **删除单条:**
   - 打开历史下拉
   - 点击某条历史的删除按钮
   - 确认该条从列表移除
   - 立即生效，无需等待

2. **清空全部:**
   - 确保有多条历史
   - 点击 "清空全部历史" 按钮
   - 确认对话框显示
   - 点击取消 -> 列表不变
   - 点击确认 -> 列表清空
   - Toast 提示 "搜索历史已清空"

3. **空状态:**
   - 清空历史后
   - 打开下拉
   - 确认显示 "暂无搜索历史"

4. **完整流程:**
   - 执行多次搜索
   - 查看历史列表
   - 删除部分记录
   - 使用历史快速填充
   - 最终清空全部
</verification>

<success_criteria>
- [ ] SearchHistoryDropdown 支持删除单条
- [ ] 底部显示清空全部按钮
- [ ] 清空前显示确认对话框
- [ ] 确认按钮使用红色 (危险操作)
- [ ] 清空成功后 Toast 提示
- [ ] 乐观更新立即生效
- [ ] flutter analyze 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-search-ui/09-04-SUMMARY.md`
</output>
