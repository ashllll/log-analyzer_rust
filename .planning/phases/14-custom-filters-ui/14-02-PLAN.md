---
phase: 14-custom-filters-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart
  - log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart
autonomous: true
requirements:
  - FILTER-04

must_haves:
  truths:
    - "用户可以点击侧边栏 + 按钮创建新过滤器"
    - "FilterEditorDialog 复用 FilterPalette 组件"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart"
      provides: "侧边栏过滤器列表和创建功能"
      contains: "_showCreateDialog"
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart"
      provides: "过滤器编辑器对话框"
      contains: "FilterPalette"
  key_links:
    - from: "saved_filters_sidebar.dart"
      to: "filter_editor_dialog.dart"
      via: "FilterEditorDialog.show() 调用"
      pattern: "FilterEditorDialog.show"
    - from: "filter_editor_dialog.dart"
      to: "filter_palette.dart"
      via: "import + 使用 FilterPalette 组件"
      pattern: "import.*filter_palette"
---

<objective>
修复 Phase 14 验证中发现的两个 gap:
1. 侧边栏 + 按钮的 `_showCreateDialog` 方法是 TODO stub，未实现创建过滤器功能
2. `filter_editor_dialog.dart` 未复用 FilterPalette 组件，导致代码重复
</objective>

<context>
@.planning/phases/14-custom-filters-ui/14-01-VERIFICATION.md
@.planning/phases/14-custom-filters-ui/14-01-SUMMARY.md

关键参考 - FilterEditorDialog.show() 调用方式 (来自 search_page.dart):
```dart
final result = await FilterEditorDialog.show(
  context,
  workspaceId: workspaceId,
  filter: filter,        // null 表示创建新过滤器
  currentFilters: _currentFilters,  // 当前搜索条件用于预填充
);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 _showCreateDialog 方法</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart</files>
  <action>
实现 `_showCreateDialog(BuildContext context)` 方法（第 210-213 行）:

1. 获取当前搜索页面的 FilterOptions 状态（需要从 context 读取或作为参数传入）
2. 调用 `FilterEditorDialog.show(context, workspaceId: widget.workspaceId, filter: null, currentFilters: currentFilters)`
3. 如果返回非 null 的 SavedFilter，调用 `widget.onSave?.call(result)` 保存

注意: 需要获取当前搜索条件用于预填充。可以使用 InheritedWidget 或传递回调来获取 SearchPage 的 _currentFilters。

参考 search_page.dart 第 1209-1214 行的实现模式。
  </action>
  <verify>代码审查: _showCreateDialog 方法不再包含 TODO，调用 FilterEditorDialog.show()</verify>
  <done>用户点击侧边栏 + 按钮时，弹出过滤器创建对话框</done>
</task>

<task type="auto">
  <name>Task 2: FilterEditorDialog 导入并使用 FilterPalette</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart</files>
  <action>
1. 在文件顶部添加导入: `import 'filter_palette.dart';`

2. 找到时间范围选择器（line 486-550）、日志级别选择器（line 553-608）、文件模式输入框（line 611-635）的代码

3. 将这些重复的 UI 代码替换为 FilterPalette 组件:
```dart
FilterPalette(
  filterOptions: _filterOptions,
  onFilterOptionsChanged: (newOptions) {
    setState(() {
      _filterOptions = newOptions;
    });
  },
)
```

4. 确保 _filterOptions 是 FilterOptions 类型（来自 common.dart）

5. 删除不再需要的重复 UI 代码和方法
  </action>
  <verify>filter_editor_dialog.dart 导入 filter_palette.dart，代码中使用 FilterPalette 组件</verify>
  <done>FilterEditorDialog 复用 FilterPalette 组件，消除代码重复</done>
</task>

</tasks>

<verification>
验证两个 gap 已关闭:
- Gap 1: 用户可以在侧边栏点击 + 按钮创建新过滤器
- Gap 2: filter_editor_dialog 使用 FilterPalette 组件
</verification>

<success_criteria>
- _showCreateDialog 方法已实现，调用 FilterEditorDialog.show() 创建过滤器
- filter_editor_dialog.dart 导入并使用 FilterPalette 组件
- 代码编译通过，无新增警告
</success_criteria>

<output>
完成后创建 `.planning/phases/14-custom-filters-ui/14-02-SUMMARY.md`
</output>
