---
phase: 03-search-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/search_page.dart
  - log-analyzer_flutter/lib/features/search/presentation/widgets/search_progress_bar.dart
autonomous: true
requirements:
  - SEARCH-03
  - UI-03

must_haves:
  truths:
    - 用户可以按日期范围筛选搜索结果
    - 用户可以看到任务进度（进度条+已扫描文件+已找到结果）
    - 用户可以通过 Ctrl+F 聚焦搜索框
    - 搜索框点击搜索按钮执行（不自动防抖）
  artifacts:
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/search_progress_bar.dart"
      provides: "搜索进度显示组件"
      min_lines: 60
    - path: "log-analyzer_flutter/lib/features/search/presentation/search_page.dart"
      provides: "搜索页面主逻辑，包含日期选择器和进度监听"
      contains: "DatePickerDialog"
  key_links:
    - from: "search_progress_bar.dart"
      to: "eventStreamService.searchSummary"
      via: "StreamBuilder"
      pattern: "StreamBuilder<SearchResultSummary>"
---

<objective>
增强搜索页面核心功能：日期范围选择器改为 DatePickerDialog、实现任务进度显示组件、添加 Ctrl+F 快捷键聚焦搜索框、搜索按钮点击执行（移除防抖）。
</objective>

<context>
@.planning/phases/03-search-features/03-CONTEXT.md
@.planning/phases/03-search-features/03-RESEARCH.md
@log-analyzer_flutter/lib/features/search/presentation/search_page.dart
@log-analyzer_flutter/lib/features/search/presentation/widgets/filter_palette.dart
@log-analyzer_flutter/lib/shared/services/event_stream_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建搜索进度条组件</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/search_progress_bar.dart</files>
  <action>
创建 SearchProgressBar 组件，实现以下功能：
- 显示线性进度条（0-100%）
- 显示已扫描文件数 (scannedFiles)
- 显示已找到结果数 (resultsFound)
- 显示取消按钮 (onCancel callback)
- 搜索完成后显示"完成"状态，几秒后自动消失
- 页面顶部固定显示，背景色 AppColors.bgCard，底部边框

参考 RESEARCH.md 中的代码示例：
```dart
class SearchProgressBar extends StatelessWidget {
  final int progress;           // 0-100
  final int scannedFiles;
  final int resultsFound;
  final VoidCallback? onCancel;
  // ... 实现
}
```
  </action>
  <verify>ls log-analyzer_flutter/lib/features/search/presentation/widgets/search_progress_bar.dart</verify>
  <done>SearchProgressBar 组件创建完成，支持进度显示和取消功能</done>
</task>

<task type="auto">
  <name>Task 2: 增强 FilterPalette 日期选择器</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/filter_palette.dart</files>
  <action>
修改 FilterPalette 中的时间范围部分，将 TextField 文本输入改为 DatePickerDialog：
- 使用 showDateRangePicker 弹出日期选择器
- 保留文本输入作为辅助显示（只读）
- 点击文本框触发日期选择器
- CONTEXT 要求：日期范围选择器自动更新为日志文件的总时间范围（未来扩展）

关键修改：
1. 将 _timeStartController 和 _timeEndController 改为 TextEditingController（只读显示）
2. 添加 _selectDateRange() 方法调用 showDateRangePicker
3. 修改 TextField 的 onTap 触发日期选择器
  </action>
  <verify>grep -n "showDateRangePicker" log-analyzer_flutter/lib/features/search/presentation/widgets/filter_palette.dart</verify>
  <done>日期选择器改为 DatePickerDialog，支持可视化日期范围选择</done>
</task>

<task type="auto">
  <name>Task 3: 集成进度条到搜索页面</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
修改 SearchPage，集成 SearchProgressBar 组件：
1. 导入 SearchProgressBar
2. 在搜索栏下方添加进度条显示（_isSearching 时显示）
3. 监听 eventStreamService.searchSummary 流获取进度数据
4. 将 progress/scannedFiles/resultsFound 传递给 SearchProgressBar
5. 添加取消搜索功能（调用 API 取消或设置标志位）

同时修复搜索逻辑：
- 移除 onChanged 防抖，改为点击搜索按钮执行
- 搜索框按 Enter 或点击搜索按钮时才执行搜索
- 不显示清空按钮（CONTEXT 要求）
  </action>
  <verify>grep -n "SearchProgressBar" log-analyzer_flutter/lib/features/search/presentation/search_page.dart</verify>
  <done>SearchPage 集成进度条，监听搜索进度事件实时更新 UI</done>
</task>

<task type="auto">
  <name>Task 4: 添加 Ctrl+F 快捷键聚焦搜索框</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
在 SearchPage 添加键盘快捷键支持：
- 使用 Focus 组件或 KeyboardListener 监听全局键盘事件
- 当按下 Ctrl+F (Windows) 或 Cmd+F (macOS) 时，聚焦搜索输入框
- _focusNode.requestFocus() 实现聚焦

实现方式：
```dart
KeyboardListener(
  focusNode: FocusNode(),
  onKeyEvent: (event) {
    if (event.logicalKey == LogicalKeyboardKey.keyF &&
        (HardwareKeyboard.instance.isControlPressed ||
         HardwareKeyboard.instance.isMetaPressed)) {
      _searchFocusNode.requestFocus();
    }
  },
  child: ...
)
```
  </action>
<verify>grep -n "LogicalKeyboardKey.keyF" log-analyzer_flutter/lib/features/search/presentation/search_page.dart</verify>
  <done>支持 Ctrl+F / Cmd+F 快捷键聚焦搜索框</done>
</task>

</tasks>

<verification>
1. 启动 Flutter 应用: `cd log-analyzer_flutter && flutter run`
2. 导航到搜索页面
3. 按 Ctrl+F 验证搜索框聚焦
4. 输入关键词，点击搜索按钮执行搜索
5. 观察进度条显示（扫描文件数、结果数）
6. 点击过滤器，验证日期选择器弹出
</verification>

<success_criteria>
- SearchProgressBar 组件正确显示进度/文件数/结果数
- 日期选择器使用 DatePickerDialog 可视化选择
- Ctrl+F / Cmd+F 可以聚焦搜索框
- 点击搜索按钮执行搜索（无防抖延迟）
- 搜索进行时显示进度条，完成后几秒消失
</success_criteria>

<output>
完成后创建 `.planning/phases/03-search-features/03-01-SUMMARY.md`
</output>
