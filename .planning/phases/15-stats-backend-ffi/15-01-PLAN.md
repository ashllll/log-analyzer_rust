---
phase: 15-stats-backend-ffi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer/src-tauri/src/ffi/commands_bridge.rs
  - log-analyzer/src-tauri/src/ffi/bridge.rs
  - log-analyzer_flutter/lib/shared/services/bridge_service.dart
  - log-analyzer_flutter/lib/shared/providers/log_level_stats_provider.dart
autonomous: true
requirements:
  - STATS-01
  - STATS-03

must_haves:
  truths:
    - "Flutter 可以通过 FFI 获取每个日志级别的记录数量"
    - "统计接口支持工作区参数"
    - "统计数据在索引更新后可通过刷新获取最新数据"
  artifacts:
    - "log-analyzer/src-tauri/src/ffi/commands_bridge.rs"
    - "log-analyzer/src-tauri/src/ffi/bridge.rs"
    - "log-analyzer_flutter/lib/shared/services/bridge_service.dart"
    - "log-analyzer_flutter/lib/shared/providers/log_level_stats_provider.dart"
  key_links:
    - "log_level_stats_provider -> bridge_service -> FFI"
---

<objective>
实现日志级别统计后端 FFI 接口，使 Flutter 应用能够通过 FFI 调用 Rust 后端获取日志级别统计
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/v1.3-log-level-stats.md
@log-analyzer/src-tauri/src/search_engine/schema.rs
@log-analyzer_flutter/lib/shared/services/bridge_service.dart

## LogLevel 定义 (from value_objects.rs)
- LogLevel::Trace (severity 0)
- LogLevel::Debug (severity 1)
- LogLevel::Info (severity 2)
- LogLevel::Warn (severity 3)
- LogLevel::Error (severity 4)
- LogLevel::Fatal (severity 5)
- LogLevel::Unknown(String)

## Tantivy Schema Level Field
Level 字段已配置为 faceted search:
```rust
let level = schema_builder.add_text_field(
    "level",
    TextOptions::default()
        .set_indexing_options(...)
        .set_fast(Some("raw")) // Enable fast field for faceting
        .set_stored(),
);
```

## 现有 FFI 模式 (from filters)
Rust FFI 函数:
- `ffi_save_filter(filter: SavedFilterInput) -> Result<bool, String>`
- 在 commands_bridge.rs 中实现
- 在 bridge.rs 中导出

Flutter BridgeService:
- `Future<bool> saveFilter(SavedFilter filter)`
- 调用 `ffi.saveFilter(filter: filter.toFfiMap())`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 Rust FFI 命令 get_log_level_stats</name>
  <files>log-analyzer/src-tauri/src/ffi/commands_bridge.rs</files>
  <action>
在 commands_bridge.rs 中添加 `ffi_get_log_level_stats` 函数:

1. 创建 LogLevelStatsInput 结构体 (workspace_id: String)
2. 创建 LogLevelStatsOutput 结构体:
   ```rust
   pub struct LogLevelStatsOutput {
       pub fatal_count: u64,
       pub error_count: u64,
       pub warn_count: u64,
       pub info_count: u64,
       pub debug_count: u64,
       pub trace_count: u64,
       pub unknown_count: u64,
       pub total: u64,
   }
   ```
3. 实现函数逻辑:
   - 获取工作区的 SearchEngineManager
   - 对每个日志级别 (FATAL, ERROR, WARN, INFO, DEBUG, TRACE) 执行 TermQuery 计数
   - 计算 total
   - 返回 LogLevelStatsOutput
4. 遵循现有 FFI 错误处理模式

参考现有实现: ffi_save_filter 函数模式
</action>
  <verify>cargo check --manifest-path log-analyzer/Cargo.toml</verify>
  <done>Rust FFI 命令添加完成，可编译</done>
</task>

<task type="auto">
  <name>Task 2: 在 bridge.rs 中导出新命令</name>
  <files>log-analyzer/src-tauri/src/ffi/bridge.rs</files>
  <action>
在 bridge.rs 中添加 FFI 导出:

1. 添加 use 语句引入新结构体
2. 在 initialize_ffi 函数中注册新命令
3. 使用 flutter_rust_bridge 宏导出函数

参考现有导出模式 (如 saveFilter)
</action>
  <verify>cargo check --manifest-path log-analyzer/Cargo.toml</verify>
  <done>FFI 导出添加完成</done>
</task>

<task type="auto">
  <name>Task 3: 在 BridgeService 中添加调用方法</name>
  <files>log-analyzer_flutter/lib/shared/services/bridge_service.dart</files>
  <action>
在 BridgeService 类中添加方法:

1. 添加 `Future<LogLevelStats?> getLogLevelStats(String workspaceId)` 方法
2. 调用 `ffi.getLogLevelStats(workspaceId: workspaceId)`
3. 处理 FFI 未初始化情况 (返回 null)
4. 添加错误处理和调试日志

参考现有方法模式: getSavedFilters
</action>
  <verify>flutter analyze lib/shared/services/bridge_service.dart</verify>
  <done>Flutter 桥接服务方法添加完成</done>
</task>

<task type="auto">
  <name>Task 4: 创建 LogLevelStats Riverpod Provider</name>
  <files>log-analyzer_flutter/lib/shared/providers/log_level_stats_provider.dart</files>
  <action>
创建新的 Riverpod Provider:

1. 创建 LogLevelStats 数据类:
   ```dart
   class LogLevelStats {
     final int fatalCount;
     final int errorCount;
     final int warnCount;
     final int infoCount;
     final int debugCount;
     final int traceCount;
     final int unknownCount;
     final int total;
   }
   ```

2. 创建 LogLevelStatsNotifier (extends StateNotifier):
   - build(String workspaceId) - 初始加载
   - refresh() - 刷新统计数据
   - 使用 Timer.periodic 实现 5 秒自动刷新 (STATS-03 实时更新)

3. 导出 provider:
   ```dart
   final logLevelStatsProvider = StateNotifierProvider.family<...>("log level stats")
   ```

参考 SavedFiltersProvider 模式
</action>
  <verify>flutter analyze lib/shared/providers/log_level_stats_provider.dart</verify>
  <done>Riverpod Provider 创建完成，支持实时刷新</done>
</task>

</tasks>

<verification>
- [ ] Rust 编译通过: cargo check --manifest-path log-analyzer/Cargo.toml
- [ ] Flutter 分析通过: flutter analyze lib/shared/services/bridge_service.dart
- [ ] Flutter 分析通过: flutter analyze lib/shared/providers/log_level_stats_provider.dart
</verification>

<success_criteria>
1. Flutter 可以调用 BridgeService.getLogLevelStats(workspaceId) 获取统计
2. 返回每个日志级别 (FATAL, ERROR, WARN, INFO, DEBUG, TRACE, UNKNOWN) 的数量
3. LogLevelStatsProvider 支持 refresh() 方法手动刷新
4. LogLevelStatsProvider 支持 5 秒自动刷新 (STATS-03)
</success_criteria>

<output>
完成后创建 `.planning/phases/15-stats-backend-ffi/15-01-SUMMARY.md`
</output>
