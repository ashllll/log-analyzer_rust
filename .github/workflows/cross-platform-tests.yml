# 暂时禁用：与主 CI 工作流重复
# 这个工作流的功能已经在 ci.yml 中实现
# 如需重新启用，请查看 git 历史恢复之前的版本
name: Disabled (duplicate of main CI)

on:
  push:
    branches: [never]  # 永远不会触发

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow is disabled because it duplicates the main CI workflow (ci.yml). All cross-platform tests are already running in ci.yml."
