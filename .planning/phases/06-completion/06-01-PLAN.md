---
phase: 06-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/shared/services/settings_service.dart
  - log-analyzer_flutter/lib/features/settings/providers/settings_provider.dart
  - log-analyzer_flutter/lib/features/settings/providers/theme_provider.dart
  - log-analyzer_flutter/lib/features/settings/presentation/settings_page.dart
  - log-analyzer_flutter/lib/features/settings/presentation/widgets/settings_sidebar.dart
  - log-analyzer_flutter/lib/features/settings/presentation/widgets/basic_settings_tab.dart
  - log-analyzer_flutter/lib/features/settings/presentation/widgets/workspace_settings_tab.dart
  - log-analyzer_flutter/lib/features/settings/presentation/widgets/search_settings_tab.dart
  - log-analyzer_flutter/lib/features/settings/presentation/widgets/about_tab.dart
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - 用户可以访问设置界面
    - 用户可以切换主题（浅色/深色/跟随系统）并实时生效
    - 用户可以设置搜索历史记录数
    - 用户可以查看关于信息
    - 设置数据在重启后保持
  artifacts:
    - path: "log-analyzer_flutter/lib/shared/services/settings_service.dart"
      provides: "SharedPreferences 封装，命名空间式存储"
    - path: "log-analyzer_flutter/lib/features/settings/providers/settings_provider.dart"
      provides: "设置状态管理"
    - path: "log-analyzer_flutter/lib/features/settings/providers/theme_provider.dart"
      provides: "主题切换状态管理"
    - path: "log-analyzer_flutter/lib/features/settings/presentation/settings_page.dart"
      provides: "设置页面（左侧导航布局）"
  key_links:
    - from: "settings_page.dart"
      to: "settings_service.dart"
      via: "settings_provider"
      pattern: "ref.watch.*settingsProvider"
    - from: "main.dart"
      to: "theme_provider.dart"
      via: "MaterialApp.themeMode"
      pattern: "themeMode: ref.watch.*themeProvider"
---

<objective>
重构设置页面为左侧导航布局，实现主题切换、工作区设置、搜索设置、关于页面，使用 SharedPreferences 持久化设置。
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-completion/06-CONTEXT.md
@.planning/phases/06-completion/06-RESEARCH.md

# 项目现有代码
@log-analyzer_flutter/lib/shared/providers/workspace_provider.dart
@log-analyzer_flutter/lib/core/router/app_router.dart
@log-analyzer_flutter/pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SettingsService（SharedPreferences 封装）</name>
  <files>log-analyzer_flutter/lib/shared/services/settings_service.dart</files>
  <action>
创建 SettingsService 类，封装 SharedPreferences：
- 命名空间前缀 'settings.'
- 主题设置（'settings.theme': 'light' | 'dark' | 'system'）
- 最近工作区列表（'settings.recent_workspaces': JSON 数组，最多5个）
- 搜索历史限制（'settings.search_history_limit': 整数，默认50）
- 最后工作区 ID（'settings.last_workspace_id': 用于启动恢复）
- 导出/导入功能（JSON 格式）
- 数据迁移支持（version 字段）

参考 RESEARCH.md 中的 Pattern 2 实现。
  </action>
  <verify>ls log-analyzer_flutter/lib/shared/services/settings_service.dart</verify>
  <done>SettingsService 类创建完成，包含所有设置项的 getter/setter</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SettingsProvider 和 ThemeProvider</name>
  <files>
    - log-analyzer_flutter/lib/features/settings/providers/settings_provider.dart
    - log-analyzer_flutter/lib/features/settings/providers/theme_provider.dart
  </files>
  <action>
创建两个 Riverpod Provider：

1. **settings_provider.dart**:
   - 使用 StateNotifier 管理设置状态
   - 加载/保存设置到 SettingsService
   - 提供 recentWorkspaces, searchHistoryLimit 等状态

2. **theme_provider.dart**:
   - 使用 StateNotifier<ThemeMode> 管理主题
   - 从 SettingsService 加载主题设置
   - 提供 setTheme(ThemeMode) 方法，实时生效
   - 使用 flutter_riverpod 的 notifier 模式

参考 RESEARCH.md 中的 Pattern 3 实现主题切换。
  </action>
  <verify>ls log-analyzer_flutter/lib/features/settings/providers/settings_provider.dart</verify>
  <done>两个 Provider 创建完成，主题切换可实时生效</done>
</task>

<task type="auto">
  <name>Task 3: 重构 SettingsPage 为左侧导航布局</name>
  <files>
    - log-analyzer_flutter/lib/features/settings/presentation/settings_page.dart
    - log-analyzer_flutter/lib/features/settings/presentation/widgets/settings_sidebar.dart
    - log-analyzer_flutter/lib/features/settings/presentation/widgets/basic_settings_tab.dart
    - log-analyzer_flutter/lib/features/settings/presentation/widgets/workspace_settings_tab.dart
    - log-analyzer_flutter/lib/features/settings/presentation/widgets/search_settings_tab.dart
    - log-analyzer_flutter/lib/features/settings/presentation/widgets/about_tab.dart
  </files>
  <action>
重构设置页面为左侧导航布局（参考 RESEARCH.md Pattern 1）：

1. **settings_page.dart**:
   - 使用 Row 布局，左侧 NavigationRail + 右侧内容
   - NavigationRail 可折叠（窗口变窄时自动折叠）
   - 默认显示「基础设置」（index 0）

2. **settings_sidebar.dart** (新文件):
   - NavigationRail 组件
   - 四个分类：基础设置、工作区设置、搜索设置、关于

3. **basic_settings_tab.dart** (新文件):
   - 主题切换 SegmentedButton（三选项：浅色/深色/跟随系统）
   - 实时生效，使用 ThemeProvider

4. **workspace_settings_tab.dart** (新文件):
   - 显示最近5个工作区列表
   - 清空历史按钮

5. **search_settings_tab.dart** (新文件):
   - 搜索历史记录数滑块（10-200，默认50）
   - 立即保存

6. **about_tab.dart** (新文件):
   - 应用名称、版本号、版权信息

使用 lucide_icons_flutter 图标包。
  </action>
  <verify>flutter analyze lib/features/settings/ 2>/dev/null || echo "flutter not available, verify file exists"</verify>
  <done>设置页面重构完成，左侧导航可正常切换四个分类</done>
</task>

</tasks>

<verification>
1. 检查 settings_service.dart 包含所有设置项的 getter/setter
2. 检查 theme_provider.dart 实现了主题实时切换
3. 检查 settings_page.dart 使用 NavigationRail 左侧导航布局
4. 检查四个 tab 组件都存在且功能完整
</verification>

<success_criteria>
- 设置页面使用左侧 NavigationRail 导航
- 四个分类可切换（基础/工作区/搜索/关于）
- 主题切换实时生效（light/dark/system）
- 设置持久化到 SharedPreferences
</success_criteria>

<output>
完成后创建 `.planning/phases/06-completion/06-01-SUMMARY.md`
</output>
