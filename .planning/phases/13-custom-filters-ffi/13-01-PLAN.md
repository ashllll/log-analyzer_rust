---
phase: 13-custom-filters-ffi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer/src-tauri/src/ffi/types.rs
  - log-analyzer/src-tauri/src/ffi/commands_bridge.rs
  - log-analyzer/src-tauri/src/ffi/bridge.rs
  - log-analyzer_flutter/lib/shared/services/bridge_service.dart
  - log-analyzer_flutter/lib/shared/models/saved_filter.dart
  - log-analyzer_flutter/lib/shared/providers/saved_filters_provider.dart
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
  - FILTER-03
  - FILTER-05
---

<objective>
实现自定义过滤器的后端 FFI 接口，使 Flutter 应用能够通过 FFI 调用 Rust 后端的过滤器 CRUD 接口。

Purpose: 为 Phase 14 的过滤器 UI 提供后端支持，实现过滤器的持久化存储。

Output: 完整的过滤器 FFI 接口链（Rust 后端 -> Flutter 桥接 -> Riverpod Provider）
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/v1.3-custom-filters.md

# 参考实现模式（来自 SearchHistory）

从 SearchHistoryProvider 和 bridge_service.dart 中提取的关键模式:

1. FFI 类型定义 (ffi/types.rs):
```rust
#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct SearchHistoryData {
    pub query: String,
    pub workspace_id: String,
    pub result_count: i32,
    pub searched_at: String,
}
```

2. FFI 命令适配 (ffi/commands_bridge.rs):
```rust
pub fn ffi_get_search_history(
    workspace_id: Option<String>,
    limit: Option<usize>,
) -> Result<Vec<SearchHistoryData>, String>
```

3. Flutter 端模型 (shared/providers/search_history_provider.dart):
```dart
class SearchHistoryItem {
  final String query;
  final String workspaceId;
  final int resultCount;
  final String searchedAt;

  factory SearchHistoryItem.fromFfi(ffi_types.SearchHistoryData data) {...}
  ffi_types.SearchHistoryData toFfi() {...}
}
```

4. Flutter Provider 模式:
```dart
@riverpod
class SearchHistory extends _$SearchHistory {
  @override
  AsyncValue<List<SearchHistoryItem>> build(String workspaceId) {
    Future.microtask(() => _loadHistory());
    return const AsyncLoading();
  }
}
```

# SavedFilter 数据模型（来自 v1.3-custom-filters.md 研究）

```dart
class SavedFilter {
  final String id;
  final String name;
  final String? description;
  final String workspaceId;
  final List<SearchTerm> terms;
  final QueryOperatorData globalOperator;
  final TimeRange? timeRange;
  final List<String> levels;
  final String? filePattern;
  final DateTime createdAt;
  final DateTime? lastUsedAt;
  final int usageCount;
  final bool isDefault;
  final int sortOrder;
}
```

# Rust 过滤器存储表结构（研究建议）

```sql
CREATE TABLE saved_filters (
    id TEXT PRIMARY KEY,
    workspace_id TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    terms_json TEXT NOT NULL,
    global_operator TEXT NOT NULL,
    time_range_start TEXT,
    time_range_end TEXT,
    levels_json TEXT,
    file_pattern TEXT,
    is_default INTEGER DEFAULT 0,
    sort_order INTEGER DEFAULT 0,
    usage_count INTEGER DEFAULT 0,
    created_at TEXT NOT NULL,
    last_used_at TEXT
);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 Rust 后端 SavedFilter FFI 类型</name>
  <files>log-analyzer/src-tauri/src/ffi/types.rs</files>
  <action>
在 ffi/types.rs 中添加 SavedFilter 相关的 FFI 类型定义:

1. 添加 SearchTermData 结构体 (term_id, value, operator, is_regex, priority, enabled, case_sensitive)
2. 添加 TimeRangeData 结构体 (start, end)
3. 添加 SavedFilterData 结构体 (id, name, description, workspace_id, terms, global_operator, time_range, levels, file_pattern, is_default, sort_order, usage_count, created_at, last_used_at)
4. 添加 SavedFilterInput 结构体（用于创建/更新过滤器）
5. 实现 From 转换 trait

参考现有 SearchHistoryData 的实现模式。
  </action>
  <verify>cargo check --lib 编译通过</verify>
  <done>types.rs 包含 SavedFilterData、SavedFilterInput、SearchTermData、TimeRangeData 四个结构体</done>
</task>

<task type="auto">
  <name>Task 2: 实现 Rust 后端过滤器 FFI 命令</name>
  <files>log-analyzer/src-tauri/src/ffi/commands_bridge.rs</files>
  <action>
在 ffi/commands_bridge.rs 中添加过滤器 CRUD FFI 函数:

1. ffi_save_filter: 保存或更新过滤器（基于 workspace_id + name 唯一键）
   - 参数: SavedFilterInput
   - 返回: Result<bool, String>
   - 使用配置文件存储（参考 ffi_save_config 模式）

2. ffi_get_saved_filters: 获取工作区的所有过滤器
   - 参数: workspace_id, limit (可选)
   - 返回: Result<Vec<SavedFilterData>, String>

3. ffi_delete_filter: 删除指定过滤器
   - 参数: filter_id, workspace_id
   - 返回: Result<bool, String>

4. ffi_update_filter_usage: 更新过滤器使用次数和最后使用时间
   - 参数: filter_id, workspace_id
   - 返回: Result<bool, String>

参考现有 ffi_get_keywords 和 ffi_save_config 的实现模式（使用配置文件存储）。
  </action>
  <verify>cargo check --lib 编译通过</verify>
  <done>commands_bridge.rs 包含四个过滤器 FFI 函数，编译通过</done>
</task>

<task type="auto">
<parameter name>Task 3: 暴露过滤器 FFI 接口到 bridge.rs</name>
  <files>log-analyzer/src-tauri/src/ffi/bridge.rs</files>
  <action>
在 ffi/bridge.rs 中注册过滤器 FFI 函数供 Flutter 调用:

1. 注册 save_filter 函数
2. 注册 get_saved_filters 函数
3. 注册 delete_filter 函数
4. 注册 update_filter_usage 函数

确保参数命名使用 camelCase 以匹配 Flutter 约定。
  </action>
  <verify>cargo check --lib 编译通过</verify>
  <done>bridge.rs 包含四个过滤器 FFI 函数声明</done>
</task>

<task type="auto">
  <name>Task 4: 创建 Flutter SavedFilter 数据模型</name>
  <files>log-analyzer_flutter/lib/shared/models/saved_filter.dart</files>
  <action>
创建 Flutter 端的 SavedFilter 数据模型文件:

1. 创建 SavedFilter 类（对应 Rust SavedFilterData）
2. 创建 SearchTerm 类（对应 Rust SearchTermData）
3. 创建 TimeRange 类（对应 Rust TimeRangeData）
4. 实现 fromFfi() 工厂方法从 FFI 数据转换
5. 实现 toFfi() 方法转换为 FFI 数据

参考 SearchHistoryItem 的实现模式。
  </action>
  <verify>flutter analyze lib/shared/models/saved_filter.dart 无错误</verify>
  <done>saved_filter.dart 文件创建，包含 SavedFilter、SearchTerm、TimeRange 类</done>
</task>

<task type="auto">
  <name>Task 5: 在 BridgeService 中添加过滤器方法</name>
  <files>log-analyzer_flutter/lib/shared/services/bridge_service.dart</files>
  <action>
在 BridgeService 中添加四个过滤器 FFI 方法:

1. saveFilter: 保存或更新过滤器
   - 参数: SavedFilter filter
   - 返回: Future<bool>

2. getSavedFilters: 获取工作区过滤器列表
   - 参数: String workspaceId, int? limit
   - 返回: Future<List<SavedFilter>>

3. deleteFilter: 删除过滤器
   - 参数: String filterId, String workspaceId
   - 返回: Future<bool>

4. updateFilterUsage: 更新过滤器使用统计
   - 参数: String filterId, String workspaceId
   - 返回: Future<bool>

参考现有 getSearchHistory 和 addSearchHistory 的实现模式。
  </action>
  <verify>flutter analyze lib/shared/services/bridge_service.dart 无错误</verify>
  <done>BridgeService 包含四个过滤器方法</done>
</task>

<task type="auto">
  <name>Task 6: 创建 SavedFilters Riverpod Provider</name>
  <files>log-analyzer_flutter/lib/shared/providers/saved_filters_provider.dart</files>
  <action>
创建 Riverpod Provider 管理过滤器状态:

1. 创建 bridgeServiceProvider 引用
2. 创建 SavedFiltersNotifier 类（继承自 AutoDisposeAsyncNotifier）
   - build(workspaceId): 加载工作区过滤器列表
   - saveFilter(SavedFilter): 乐观更新 + 后端同步
   - deleteFilter(String filterId): 乐观更新 + 后端同步
   - updateFilterUsage(String filterId): 更新使用统计

3. 创建 savedFiltersProvider(workspaceId) provider

参考 SearchHistory provider 的实现模式。
  </action>
  <verify>flutter analyze lib/shared/providers/saved_filters_provider.dart 无错误</verify>
  <done>saved_filters_provider.dart 包含 SavedFiltersNotifier 类和 provider</done>
</task>

</tasks>

<verification>
1. Rust 编译: cargo check --lib 在 log-analyzer/src-tauri 通过
2. Flutter 分析: flutter analyze 在 log-analyzer_flutter 通过
3. 类型一致性: SavedFilter Dart 模型字段与 Rust SavedFilterData 一致
4. 业务逻辑覆盖:
   - FILTER-01 (创建): saveFilter 方法实现
   - FILTER-02 (编辑): saveFilter 方法实现（更新现有）
   - FILTER-03 (删除): deleteFilter 方法实现
   - FILTER-05 (持久化): 配置文件存储实现
</verification>

<success_criteria>
1. 可以创建新过滤器（名称 + 条件组合）
   - saveFilter 方法接受 SavedFilter 对象，包含 name, terms, timeRange, levels, filePattern
2. 可以编辑现有过滤器
   - saveFilter 方法支持更新（按 id 更新）
3. 可以删除过滤器
   - deleteFilter 方法按 filter_id 删除
4. 过滤器在工作区级别持久化存储
   - 配置文件存储，每个工作区独立
</success_criteria>

<output>
After completion, create `.planning/phases/13-custom-filters-ffi/13-01-SUMMARY.md`
</output>
