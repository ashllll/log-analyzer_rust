---
phase: 06-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/splash/splash_page.dart
  - log-analyzer_flutter/lib/shared/services/settings_service.dart
  - log-analyzer_flutter/lib/shared/widgets/empty_state_widget.dart
  - log-analyzer_flutter/lib/core/theme/app_theme.dart
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - 应用启动时显示 Splash 画面
    - 应用启动时尝试恢复上次工作区
    - 上次工作区不存在时显示工作区列表
    - 空状态场景有合适的提示
  artifacts:
    - path: "log-analyzer_flutter/lib/features/splash/splash_page.dart"
      provides: "启动画面 + 工作区自动恢复"
    - path: "log-analyzer_flutter/lib/shared/widgets/empty_state_widget.dart"
      provides: "通用空状态组件"
  key_links:
    - from: "splash_page.dart"
      to: "workspace_provider.dart"
      via: "loadWorkspaceById()"
      pattern: "await ref.read(workspaceStateProvider.notifier).loadWorkspaceById"
    - from: "splash_page.dart"
      to: "settings_service.dart"
      via: "getLastWorkspaceId()"
      pattern: "prefs.getString.*last_workspace_id"
---

<objective>
实现应用启动时的工作区自动恢复功能，添加通用空状态组件，完善用户体验。
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-completion/06-CONTEXT.md
@.planning/phases/06-completion/06-RESEARCH.md

# 项目现有代码
@log-analyzer_flutter/lib/features/splash/splash_page.dart
@log-analyzer_flutter/lib/shared/providers/workspace_provider.dart
@log-analyzer_flutter/lib/core/router/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 SplashPage 实现工作区自动恢复</name>
  <files>log-analyzer_flutter/lib/features/splash/splash_page.dart</files>
  <action>
扩展 SplashPage 的 _initialize() 方法（参考 RESEARCH.md Pattern 4）：

1. FFI 初始化成功后
2. 调用 ref.read(appStateProvider.notifier).loadConfig() 加载配置
3. 尝试从 SharedPreferences 获取最后工作区 ID：
   - key: 'settings.last_workspace_id'
4. 如果存在最后工作区 ID：
   - 检查工作区列表中是否存在该 ID
   - 如果存在，调用 loadWorkspaceById() 恢复
   - 恢复成功后跳转到 /search
5. 如果无法恢复（不存在或加载失败）：
   - 跳转到 /workspaces 显示工作区列表
6. 错误处理：
   - TimeoutException: 显示"连接超时"错误
   - 其他异常: 显示"连接失败"错误
   - 失败时提供"重试"按钮

保持现有代码结构，只扩展 _initialize() 方法。
  </action>
  <verify>grep -n "last_workspace_id" log-analyzer_flutter/lib/features/splash/splash_page.dart</verify>
  <done>SplashPage 扩展完成，支持工作区自动恢复</done>
</task>

<task type="auto">
  <name>Task 2: 创建通用 EmptyStateWidget 组件</name>
  <files>log-analyzer_flutter/lib/shared/widgets/empty_state_widget.dart</files>
  <action>
创建通用空状态组件（参考 RESEARCH.md 中的"空状态组件"示例）：

1. 组件参数：
   - icon: IconData（必填）
   - title: String（必填）
   - description: String?（可选）
   - actionLabel: String?（可选）
   - onAction: VoidCallback?（可选）

2. 布局：
   - 居中布局
   - 图标 + 标题 + 描述 + 操作按钮（可选）
   - 使用项目现有的颜色常量（AppColors）

3. 样式：
   - 图标大小 64，颜色 muted
   - 标题 18px，字重 600
   - 描述 14px，灰色

导出到 widgets.dart 供其他页面使用。
  </action>
  <verify>ls log-analyzer_flutter/lib/shared/widgets/empty_state_widget.dart</verify>
  <done>EmptyStateWidget 组件创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 在 main.dart 中集成 ThemeProvider</name>
  <files>log-analyzer_flutter/lib/main.dart</files>
  <action>
修改 main.dart 以集成 ThemeProvider：

1. 在 ProviderScope 下添加 themeMode 监听
2. 在 MaterialApp 中使用 themeMode: ref.watch(themeProvider)
3. 确保主题可以实时切换生效

如果 main.dart 已正确配置，则跳过此任务。

参考 RESEARCH.md Pattern 3 中的 "main.dart 中使用" 示例。
  </action>
  <verify>grep -n "themeMode" log-analyzer_flutter/lib/main.dart || echo "Need to check main.dart"</verify>
  <done>main.dart 正确集成 ThemeProvider</done>
</task>

</tasks>

<verification>
1. 检查 splash_page.dart 包含工作区恢复逻辑
2. 检查 empty_state_widget.dart 组件完整
3. 检查 main.dart 集成了 themeMode
</verification>

<success_criteria>
- 应用启动时尝试恢复上次工作区
- 工作区恢复失败时跳转到工作区列表
- 空状态组件可在其他页面复用
- 主题切换实时生效
</success_criteria>

<output>
完成后创建 `.planning/phases/06-completion/06-02-SUMMARY.md`
</output>
