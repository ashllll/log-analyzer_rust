---
phase: 03-search-features
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/widgets/log_detail_panel.dart
  - log-analyzer_flutter/lib/features/search/presentation/search_page.dart
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - UI-01
  - UI-02

must_haves:
  truths:
    - 用户可以看到搜索结果列表（文件名+时间戳+日志级别+完整日志行）
    - 搜索结果中关键词高亮显示（不同关键词不同颜色）
    - 用户可以点击日志行查看详情（全屏详情视图）
    - 详情面板显示无限上下文
    - 详情面板支持 Esc 键关闭
  artifacts:
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/log_detail_panel.dart"
      provides: "日志详情面板组件"
      min_lines: 80
    - path: "log-analyzer_flutter/lib/features/search/presentation/search_page.dart"
      provides: "搜索页面主逻辑，集成详情面板"
      contains: "LogDetailPanel"
  key_links:
    - from: "search_page.dart LogRowWidget"
      to: "log_detail_panel.dart"
      via: "_selectLog() -> showDialog"
      pattern: "showDialog.*LogDetailPanel"
---

<objective>
实现搜索结果展示增强：关键词高亮优化、日志详情面板（无限上下文）、点击高亮关键词跳转到详情。
</objective>

<context>
@.planning/phases/03-search-features/03-CONTEXT.md
@.planning/phases/03-search-features/03-RESEARCH.md
@log-analyzer_flutter/lib/features/search/presentation/search_page.dart
@log-analyzer_flutter/lib/features/search/presentation/widgets/log_row_widget.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建日志详情面板组件</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/log_detail_panel.dart</files>
  <action>
创建 LogDetailPanel 组件，实现以下功能：
- 全屏详情视图（Dialog 宽度 90% 视口，高度 90% 视口）
- 显示文件名、时间戳、日志级别
- 显示完整日志行内容（monospace 字体）
- 显示无限上下文（前后各加载一定范围的行）
- Esc 键关闭详情面板
- 点击列表行切换详情显示

参考 RESEARCH.md 中的代码示例：
```dart
class LogDetailPanel extends StatefulWidget {
  final LogEntry entry;
  final VoidCallback onClose;
  // ... 实现
}
```

关键实现：
1. 使用 StatefulWidget 管理上下文加载状态
2. 初始化时加载前后 100 行上下文（滑动窗口）
3. KeyboardListener 监听 Escape 键关闭
4. 高亮匹配的关键词
5. 点击日志行时通过 showDialog 显示详情
  </action>
  <verify>ls log-analyzer_flutter/lib/features/search/presentation/widgets/log_detail_panel.dart</verify>
  <done>LogDetailPanel 组件创建完成，支持无限上下文和 Esc 关闭</done>
</task>

<task type="auto">
  <name>Task 2: 集成详情面板到搜索页面</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
修改 SearchPage，集成 LogDetailPanel：

1. 导入 LogDetailPanel
2. 修改 _selectLog(int index) 方法：
   - 当已有日志选中时，显示详情面板
   - 使用 showDialog 弹出 LogDetailPanel
   - 传递 LogEntry 数据和关闭回调

3. 添加日志详情回调：
```dart
void _showLogDetail(LogEntry entry) {
  showDialog(
    context: context,
    builder: (context) => LogDetailPanel(
      entry: entry,
      onClose: () => Navigator.pop(context),
    ),
  );
}
```

4. CONTEXT 要求：点击高亮关键词跳转到详情
   - LogRowWidget 的 onTap 触发详情显示
  </action>
  <verify>grep -n "LogDetailPanel" log-analyzer_flutter/lib/features/search/presentation/search_page.dart</verify>
  <done>SearchPage 集成详情面板，点击日志行显示详情</done>
</task>

<task type="auto">
  <name>Task 3: 优化关键词高亮样式</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/log_row_widget.dart</files>
  <action>
检查并优化 LogRowWidget 的关键词高亮实现：

1. 确认不同关键词使用不同颜色（基于 hash 分配）
2. 确认背景保持暗色
3. 确认精确匹配高亮
4. 确认点击高亮关键词可以跳转到详情（通过 GestureDetector 包裹 TextSpan）

CONTEXT 要求：
- 不同关键词使用不同颜色 (随机分配)
- 背景保持暗色
- 精确匹配高亮
- 点击高亮关键词跳转到详情

如现有实现已满足要求，则此任务标记为已完成。
  </action>
  <verify>grep -n "matchedKeywords\|TextSpan" log-analyzer_flutter/lib/features/search/presentation/widgets/log_row_widget.dart</verify>
  <done>关键词高亮样式符合 CONTEXT 要求</done>
</task>

</tasks>

<verification>
1. 启动 Flutter 应用: `cd log-analyzer_flutter && flutter run`
2. 执行搜索操作
3. 点击日志行，验证详情面板弹出
4. 按 Esc 键关闭详情面板
5. 验证详情面板显示上下文内容
6. 验证关键词高亮显示
</verification>

<success_criteria>
- 点击日志行显示详情面板
- 详情面板显示完整日志行和上下文
- Esc 键可以关闭详情面板
- 关键词高亮使用不同颜色区分
- 点击高亮关键词可跳转到详情
</success_criteria>

<output>
完成后创建 `.planning/phases/03-search-features/03-02-SUMMARY.md`
</output>
