---
phase: 02-workspace-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "log-analyzer_flutter/pubspec.yaml"
  - "log-analyzer_flutter/lib/shared/providers/import_progress_provider.dart"
  - "log-analyzer_flutter/lib/shared/widgets/import_progress_dialog.dart"
  - "log-analyzer_flutter/lib/shared/widgets/drop_zone.dart"
  - "log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart"
autonomous: true
requirements:
  - FILE-01
  - FILE-07
user_setup: []

must_haves:
  truths:
    - "用户可以通过拖放导入文件夹"
    - "用户可以通过按钮选择导入文件夹"
    - "导入进度实时显示"
    - "用户可以取消导入操作"
    - "导入完成显示摘要报告"
  artifacts:
    - path: "log-analyzer_flutter/pubspec.yaml"
      provides: "desktop_drop 依赖"
    - path: "log-analyzer_flutter/lib/shared/providers/import_progress_provider.dart"
      provides: "导入进度状态管理"
    - path: "log-analyzer_flutter/lib/shared/widgets/import_progress_dialog.dart"
      provides: "导入进度模态对话框（圆形进度条、详细信息、取消按钮）"
    - path: "log-analyzer_flutter/lib/shared/widgets/drop_zone.dart"
      provides: "拖放区域组件"
  key_links:
    - from: "drop_zone.dart"
      to: "api_service.dart"
      via: "apiService.importFolder()"
      pattern: "importFolder"
    - from: "import_progress_provider.dart"
      to: "import_progress_dialog.dart"
      via: "ref.watch(importProgressProvider)"
      pattern: "importProgressProvider"
---

<objective>
实现文件夹导入功能，包括拖放支持和导入进度显示。
</objective>

<context>
@.planning/phases/02-workspace-import/02-CONTEXT.md
@.planning/phases/02-workspace-import/02-RESEARCH.md
@log-analyzer_flutter/lib/shared/services/api_service.dart
@log-analyzer_flutter/lib/shared/providers/task_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 desktop_drop 依赖</name>
  <files>log-analyzer_flutter/pubspec.yaml</files>
  <action>
添加 desktop_drop 包依赖：
1. 在 pubspec.yaml 的 dependencies 中添加：
   desktop_drop: ^0.4.0
2. 运行 flutter pub get 获取依赖
  </action>
  <verify>
    <automated>flutter test test/import/import_progress_provider_test.dart 2>&1 | head -5</automated>
  </verify>
  <done>desktop_drop 已添加到项目依赖</done>
</task>

<task type="auto">
  <name>Task 2: 创建导入进度 Provider</name>
  <files>log-analyzer_flutter/lib/shared/providers/import_progress_provider.dart</files>
  <action>
创建导入进度状态管理 Provider：
1. 创建 ImportProgressState 数据类（totalFiles, processedFiles, currentFile, progressPercent, errors, status）
2. 定义 ImportStatus 枚举（idle, importing, paused, completed, cancelled）
3. 创建 ImportProgressNotifier（StateNotifier）
4. 实现 updateProgress() 方法
5. 实现 startImport(), pauseImport(), resumeImport(), cancelImport() 方法
6. 集成任务进度 Stream（参考 task_provider.dart）
  </action>
  <verify>
    <automated>flutter test test/import/import_progress_provider_test.dart</automated>
  </verify>
  <done>ImportProgressProvider 可以管理导入进度状态</done>
</task>

<task type="auto">
  <name>Task 3: 创建拖放区域组件</name>
  <files>log-analyzer_flutter/lib/shared/widgets/drop_zone.dart</files>
  <action>
创建拖放区域组件：
1. 使用 desktop_drop 实现 DropTarget
2. 支持 onDragEntered/onDragExited/onDragDone 事件
3. 实现视觉反馈（拖入时高亮边框和背景色变化）
4. 回调 onFilesDropped 传递文件路径列表
5. 支持仅接收文件夹（.log, .txt, .json 文件过滤）
6. 导出 DropZoneWidget 组件
  </action>
  <verify>
    <automated>flutter test test/widgets/import_progress_dialog_test.dart</automated>
  </verify>
  <done>拖放区域组件可以在用户拖入文件时触发回调</done>
</task>

<task type="auto">
  <name>Task 4: 创建导入进度对话框</name>
  <files>log-analyzer_flutter/lib/shared/widgets/import_progress_dialog.dart</files>
  <action>
创建导入进度模态对话框：
1. 使用 AlertDialog 或 Dialog 组件
2. 圆形进度条（CircularProgressIndicator）
3. 显示已处理/总文件数、当前文件名、预估剩余时间
4. 显示处理速度
5. 取消按钮（导入中可取消）
6. 暂停/继续按钮
7. 错误列表显示
8. 导入完成显示摘要报告
  </action>
  <verify>
    <automated>flutter test test/widgets/import_progress_dialog_test.dart</automated>
  </verify>
  <done>导入进度对话框显示完整信息并支持取消操作</done>
</task>

<task type="auto">
  <name>Task 5: 集成拖放到工作区页面</name>
  <files>log-analyzer_flutter/lib/features/workspace/presentation/workspaces_page.dart</files>
  <action>
在工作区页面集成拖放功能：
1. 导入 DropZoneWidget 组件
2. 使用 DropZoneWidget 包裹工作区列表或内容区域
3. 在 onFilesDropped 回调中调用导入逻辑
4. 显示 ImportProgressDialog 跟踪进度
5. 添加导入按钮支持多选文件/文件夹（使用 file_picker）
6. 支持重复文件检测和提示
  </action>
  <verify>
    <automated>flutter test test/workspace/workspace_provider_test.dart test/import/import_progress_provider_test.dart</automated>
  </verify>
  <done>工作区页面支持拖放导入文件夹</done>
</task>

</tasks>

<verification>
1. cd log-analyzer_flutter && flutter pub get
2. flutter run 启动应用
3. 导航到工作区页面
4. 拖放一个文件夹到窗口，验证进度对话框出现
5. 点击导入按钮，选择文件夹，验证功能
6. 取消导入，验证状态更新
</verification>

<success_criteria>
- [x] desktop_drop 依赖已添加
- [x] 拖放区域组件实现
- [x] 导入进度对话框实现
- [x] 工作区页面集成拖放功能
</success_criteria>

<output>
完成后创建 `.planning/phases/02-workspace-import/02-02-SUMMARY.md`
</output>
