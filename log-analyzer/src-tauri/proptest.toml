# Proptest 全局配置
# 减少默认用例数量以加速 CI 测试

# 默认每个属性测试运行的用例数
# ME-56: Raised from 32 to 100 for better property test coverage
cases = 100

# 最大并发执行数
max_shrink_time = 30000  # 30秒最大收缩时间
max_shrink_iters = 5000  # 最大收缩迭代次数
