---
phase: 14-custom-filters-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart
  - log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart
  - log-analyzer_flutter/lib/features/search/presentation/widgets/filter_quick_select.dart
  - log-analyzer_flutter/lib/features/search/presentation/search_page.dart
autonomous: true
requirements:
  - FILTER-04

must_haves:
  truths:
    - "用户可以在侧边栏查看已保存的过滤器列表"
    - "用户可以创建新过滤器并保存当前搜索条件"
    - "用户可以编辑和删除现有过滤器"
    - "用户可以在搜索栏快速选择并应用过滤器"
    - "点击过滤器自动填充搜索条件并执行搜索"
  artifacts:
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart"
      provides: "侧边栏过滤器列表展示"
      min_lines: 150
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart"
      provides: "过滤器创建/编辑对话框"
      min_lines: 200
    - path: "log-analyzer_flutter/lib/features/search/presentation/widgets/filter_quick_select.dart"
      provides: "搜索栏过滤器快捷选择器"
      min_lines: 80
  key_links:
    - from: "saved_filters_sidebar.dart"
      to: "saved_filters_provider.dart"
      via: "ref.watch(savedFiltersProvider(workspaceId))"
      pattern: "savedFiltersProvider"
    - from: "filter_editor_dialog.dart"
      to: "filter_palette.dart"
      via: "复用 FilterPalette 组件"
      pattern: "FilterPalette"
    - from: "filter_quick_select.dart"
      to: "search_page.dart"
      via: "搜索栏集成"
      pattern: "onApply.*filter"
---

<objective>
实现自定义过滤器 UI 功能，包括侧边栏过滤器列表、创建/编辑对话框和搜索栏快捷访问

Purpose: 用户可以通过侧边栏和对话框管理过滤器，并在搜索时快速应用
Output:
- SavedFiltersSidebar: 侧边栏过滤器列表组件
- FilterEditorDialog: 过滤器创建/编辑对话框
- FilterQuickSelect: 搜索栏过滤器快捷选择器
- 集成到 SearchPage
</objective>

<execution_context>
@C:/Users/white/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/white/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/v1.3-custom-filters.md
@.planning/phases/13-custom-filters-ffi/13-01-SUMMARY.md
@log-analyzer_flutter/lib/shared/providers/saved_filters_provider.dart
@log-analyzer_flutter/lib/shared/models/saved_filter.dart
@log-analyzer_flutter/lib/features/search/presentation/widgets/filter_palette.dart
@log-analyzer_flutter/lib/shared/widgets/workspace_picker_dialog.dart

<interfaces>
<!-- 关键类型和契约 -->

From saved_filter.dart:
```dart
class SavedFilter {
  final String id;
  final String name;
  final String? description;
  final String workspaceId;
  final List<SearchTerm> terms;
  final String globalOperator;
  final TimeRange? timeRange;
  final List<String> levels;
  final String? filePattern;
  final bool isDefault;
  final int sortOrder;
  final int usageCount;
  final String createdAt;
  final String? lastUsedAt;
}

class SearchTerm {
  final String id;
  final String value;
  final String operator;
  final bool isRegex;
  final int priority;
  final bool enabled;
  final bool caseSensitive;
}

class TimeRange {
  final String? start;
  final String? end;
}
```

From saved_filters_provider.dart:
```dart
@riverpod
class SavedFilters extends _$SavedFilters {
  @override
  AsyncValue<List<SavedFilter>> build(String workspaceId);
  Future<bool> saveFilter(SavedFilter filter);
  Future<bool> deleteFilter(String filterId);
  Future<bool> updateFilterUsage(String filterId);
}
```

From filter_palette.dart:
```dart
typedef FilterApplyCallback = void Function({
  required TimeRange timeRange,
  required List<String> levels,
  String? filePattern,
});

class FilterPalette extends ConsumerStatefulWidget {
  final FilterApplyCallback? onApply;
  final FilterOptions? currentFilters;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SavedFiltersSidebar 侧边栏组件</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/saved_filters_sidebar.dart</files>
  <action>
创建侧边栏过滤器列表组件，实现以下功能：

1. 使用 `ref.watch(savedFiltersProvider(workspaceId))` 监听过滤器状态
2. 显示过滤器列表，包含名称、描述摘要、操作按钮
3. 支持点击应用过滤器（调用 onApply 回调）
4. 支持编辑和删除操作
5. 复用 FilterPalette 的 UI 风格（深色主题、圆角边框）

关键实现：
- 列表项显示：图标（默认/star）+ 名称 + 条件摘要
- 条件摘要构建：关键词数量 + 级别列表 + 时间范围
- 右键菜单支持编辑/删除/设为默认
- 空状态显示"暂无保存的过滤器"
- 加载状态显示 CircularProgressIndicator

参考 FilterPalette 的 AppColors 配色方案。
  </action>
  <verify>
flutter analyze lib/features/search/presentation/widgets/saved_filters_sidebar.dart</verify>
  <done>
侧边栏组件渲染成功，显示过滤器列表，点击可触发 onApply 回调
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 FilterEditorDialog 过滤器编辑对话框</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/filter_editor_dialog.dart</files>
  <action>
创建过滤器创建/编辑对话框组件，实现以下功能：

1. 支持创建新过滤器和编辑现有过滤器
2. 复用 FilterPalette 的条件选择 UI（时间范围、级别、文件模式）
3. 添加过滤器名称输入（必填，最多50字符）
4. 添加过滤器描述输入（可选，最多200字符）
5. 添加"设为默认"复选框
6. 过滤器预览：显示当前配置的摘要

对话框结构参考 WorkspacePickerDialog：
- 标题栏带图标
- 内容区：名称输入 + FilterPalette 条件 + 预览
- 底部操作按钮：取消 + 保存

保存时构建 SavedFilter 对象并调用 provider.saveFilter()
  </action>
  <verify>
flutter analyze lib/features/search/presentation/widgets/filter_editor_dialog.dart</verify>
  <done>
对话框打开正常，保存过滤器后列表更新，编辑现有过滤器可加载已有条件
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 FilterQuickSelect 搜索栏过滤器快捷选择器</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/widgets/filter_quick_select.dart</files>
  <action>
创建搜索栏过滤器快捷选择组件，实现以下功能：

1. 搜索栏右侧添加过滤器图标按钮
2. 点击显示过滤器下拉列表（最多显示5个最近使用的）
3. 显示当前激活的过滤器（Badge 提示）
4. 支持快速选择并应用过滤器
5. 支持"保存当前过滤器"按钮打开 FilterEditorDialog

UI 参考 SearchHistoryDropdown：
- 图标按钮带 Badge 指示激活状态
- 下拉列表显示过滤器名称和条件摘要
- 底部添加"保存当前过滤器"入口
  </action>
  <verify>
flutter analyze lib/features/search/presentation/widgets/filter_quick_select.dart</verify>
  <done>
搜索栏显示过滤器按钮，点击可选择过滤器，应用后触发搜索
  </done>
</task>

<task type="auto">
  <name>Task 4: 集成到 SearchPage 搜索页面</name>
  <files>log-analyzer_flutter/lib/features/search/presentation/search_page.dart</files>
  <action>
将自定义过滤器 UI 集成到搜索页面：

1. 在搜索栏区域添加 FilterQuickSelect 组件
2. 在页面布局中添加 SavedFiltersSidebar（位于 FilterPalette 上方或侧边栏区域）
3. 添加"保存当前过滤器"按钮调用 FilterEditorDialog
4. 实现过滤器应用逻辑：
   - 当用户点击过滤器时，自动填充搜索条件
   - 调用 SearchQueryProvider 执行搜索
5. 更新 SearchQueryProvider 支持接收 SavedFilter 参数

集成位置：
- FilterQuickSelect: 放在搜索输入框右侧（现有搜索模式选择器旁边）
- SavedFiltersSidebar: 放在 FilterPalette 上方作为展开面板，或集成到现有侧边栏布局
  </action>
  <verify>
flutter analyze lib/features/search/presentation/search_page.dart</verify>
  <done>
搜索页面显示过滤器快捷按钮和侧边栏列表，点击过滤器自动填充并执行搜索
  </done>
</task>

</tasks>

<verification>
- [ ] 侧边栏显示过滤器列表（SavedFiltersSidebar）
- [ ] 过滤器创建/编辑对话框可用（FilterEditorDialog）
- [ ] 搜索栏显示过滤器快捷按钮（FilterQuickSelect）
- [ ] 点击过滤器自动填充搜索条件并执行搜索
- [ ] Flutter 分析通过
- [ ] 所有组件正确导入和使用 SavedFiltersProvider
</verification>

<success_criteria>
1. 侧边栏显示过滤器列表 - SavedFiltersSidebar 组件正确渲染过滤器
2. 过滤器创建/编辑对话框 - FilterEditorDialog 支持新建和编辑过滤器
3. 搜索栏显示过滤器快捷按钮 - FilterQuickSelect 组件集成到搜索栏
4. 点击过滤器自动填充搜索条件 - 选择过滤器时自动填充时间范围、级别、文件模式等条件

满足 FILTER-04 需求：
- 用户可以通过侧边栏管理过滤器（创建/编辑/删除）
- 用户可以在搜索时快速应用过滤器
</success_criteria>

<output>
After completion, create `.planning/phases/14-custom-filters-ui/{phase}-01-SUMMARY.md`
</output>
